# PROGRESS.md — CurrentFlow durable log

Durable record of shipped slices and each module's **validation state** (RULE B). Update this
whenever a slice lands or a module changes validation state. Pairs with `PLAN.md` (the plan) and
`LOCKED_SPEC.md` (the spec).

`PAPER_VALIDATION_MONTHS = 3` (default). A module cannot display a number until `VALIDATED`.

---

## Shipped slices

| Slice | Status | Date | Notes |
|---|---|---|---|
| Spec & docs (LOCKED_SPEC v1.1, DATA_SOURCES, screeners) | ✅ | 2026-07-01 | Spec locked; data layer mapped to Stockbit `exodus`; server-side screeners defined. |
| Companion scaffolding (PLAN, CLAUDE, PROGRESS) | ✅ | 2026-07-01 | This file + PLAN.md + CLAUDE.md created per spec §14. |
| UI design handoff (hifi) | ✅ | 2026-07-01 | `design/` — README + `.dc.html` prototype; all 8 modules, RULE A/B enforced, seeded mock. Spec in bundle verified identical to root. Reference only, not shipped. |
| 1 · Data layer + integrity checks | ✅ | 2026-07-01 | `ExodusClient` (broker_summary + ohlcv_foreign; 401 fail-loud, refresh, exponential backoff); DuckDB store keyed `(symbol,date,as_of)`, ingest-once; integrity TRADED/NO_TRADES/NOT_PUBLISHED/GAP; look-ahead-safe reads. 23 tests pass. Pending live: `login/v6`+MFA transport & empirical broker publish-latency pinning (LD-5, conservative next-day fallback in force). |
| 2 · Universe gate + Broker Flow Analyzer | ✅ | 2026-07-02 | §3 hard floor + Track A/B + ARA/ARB derivation + rebalance down-weight (`universe/`); Broker Flow Analyzer observation module (`signals/broker_flow.py`) + Streamlit view (`ui/`); SCR-0 wired via screener POST, cached to `scr0_eligible` with `as_of`; DAL adds `symbol_info`/`corp_actions`/`special_board`. 60 new tests (83 total). Armed-list alerts stay manual pending 2–4 wks signal-quality validation. |
| 3 · Foreign Flow Dashboard + Money Flow Replay | ✅ | 2026-07-02 | Foreign Flow observation module (`signals/foreign_flow.py`): NBSA magnitude/persistence/reversal, vs-20d multiple + z-score (measurements), %-of-float, market/sector tide, KSEI overlay. Money Flow Replay (`signals/replay.py`): per-frame historical `decision_ts` re-reads (D+1 09:15 WIB), audit-tested against live signals. SCR-1A wired + cached with `as_of`; new DAL feed `ksei_ownership` (fetch-time `as_of`). Both wired into Streamlit. 27 new tests (110 total). |
| 4 · Phase classifier + SMS (internal) + veto | ✅ | 2026-07-02 | RULE A Wyckoff gate (`signals/phase.py`, detector-fed: climax→range→spring/SOS+LPS/UTAD; only C/D tradeable); internal SMS with locked §4 track weights (`signals/sms.py`, number GATED); full §5 veto taxonomy (`signals/veto.py`); engine gate→score→veto→ARMED (`signals/engine.py`); RULE B switch (`validation/state.py`) — SMS/Rank ships components only (`ui/sms_view.py`). Institutional Accumulation Detector + Smart Money Heatmap observation modules; replay phase lane lit. SCR-1B/1C/2 wired + cached. Backtest deferred to slice 7 (shared fill engine). 45 new tests (155 total). |
| 5 · Stage-2 distribution / trap layer | ✅ | 2026-07-02 | §8 signal-decay observation layer (`signals/distribution.py`): PHASE_ROLLOVER (UTAD/distribution), NO_DEMAND (VSA), BEARISH_DIVERGENCE (price up while flow falls — best exit), FOREIGN_OUTFLOW (NBSA sell streak) — categorical severities, no number (RULE B). `TrapMonitor` unifies §5 veto + §8 decay; `ui/trap_view.py` ribbon wired into every built view. SCR-EXIT screener wired + cached to `scr_exit_distribution` with `as_of`; `exit_flags_for` scopes to open+ARMED. 17 new tests (172 total). |
| 6 · Sector Rotation Map + Portfolio Risk Monitor | ✅ | 2026-07-02 | Sector Rotation Map derived view (`signals/sector_rotation.py`): net-foreign flow by sector on the RS-vs-flow quadrant (LEADERS/EARLY_RECOVERY/DISTRIBUTION_WARN/AVOID, §9 labels), RS vs the universe equal-weight proxy, tide; look-ahead-safe, missing≠zero. Portfolio Risk Monitor observation (`signals/risk_monitor.py`): §6 exposure caps (10%/30%), sector HHI, same-bandar crowding matrix (broker-overlap cosine) + correlated-pair check (ρ≥0.7), β vs injected benchmark, historical VaR (95%·1d), days-to-exit, scenario stress, §6 circuit breakers. Positions are an input (fill engine→slice 7); P&L withheld without an entry price. Views `ui/sector_view.py` + `ui/risk_view.py` wired into Streamlit. 20 new tests (192 total). |
| 7 · Execution (trigger/tilt/fill/risk) | ✅ | 2026-07-03 | Technical trigger (`execution/trigger.py`): Phase C spring / Phase D LPS → entry-limit + invalidation stop + first target (AR high / measured move), **R:R ≥ 2:1 or skip** (LD-3). Fundamental tilt (`fundamentals/tilt.py`): MF combined-rank tercile → COMPOUNDER/NEUTRAL/SPECULATIVE ×1.0/0.75/0.5, negative-EBIT override, FLOW_ONLY dual-track (ROE proxy, never COMPOUNDER hold) — never a gate (§7). Order gen (`execution/order.py`): LIMIT-only, 1%-risk × conviction sizing, §6 exposure caps + circuit breakers. **IDX paper fill engine** (`paper/fill.py`, §12): lot=100, tick bands, ARA/ARB adverse-side reject, next-open + tiered slippage, limit discipline, full fee stack (commission+levy+VAT+0.1% sell tax), T+2 — the ONE shared engine. Exit mgr (`execution/risk.py`, §8): stop→target→trailing→signal-decay (divergence best exit), capital-first. SCR-3/SCR-4 wired + cached. 54 new tests (246 total). Forward-paper *run* + 2-yr backtest reconciliation deferred to slice 8; live fundamentals feed not yet wired (tilt pure over injected/SCR-4 values). |
| 8 · Paper-trade validation wiring | ✅ | 2026-07-03 | Paper-trade runner (`validation/runner.py`): walks engine→trigger→tilt→order→**shared fill engine**→risk/exit into closed `PaperTrade`s; `run_backtest` (batch) + `run_forward` (day-by-day) reconcile over identical data (§13). `PaperTrade` atom (`trade.py`) — P&L net of full fee stack from the fills' cash flows. §8 metrics (`metrics.py`): net-of-fee return/Sharpe/maxDD/hit-rate/turnover + `walk_forward_sharpe`; **IHSG benchmark refused**. Promotion engine (`promotion.py`): `ValidationLedger` is the server-authoritative sole writer — promotes OBSERVATION_ONLY→VALIDATING→VALIDATED only on ≥`PAPER_VALIDATION_MONTHS` **and** positive walk-forward Sharpe. Observation↔claim switch generalized (`state.gated_display`) across all 3 gated modules (SMS + new `ui/ranking_view` + `ui/daily_top_view`), all wired into Streamlit reading the ledger. 21 new tests (267 total). A real multi-month forward run (needs live transport + accrued time) deferred; production modules stay OBSERVATION_ONLY until an actual run promotes them. |
| 10 · Live DAL transport | ✅ | 2026-07-03 | Closes the standing deferral (not a spec §11 slice; no locked-behavior change, no bump). Keychain Bearer store (`dal/token_store.py`, macOS `security` CLI, zero new dep) — missing→None never a blank header, strips `Bearer ` prefix, injectable runner. httpx transport (`dal/transport.py`) binds onto the client's injected `Transport`/`PostTransport`: returns raw `Response` (client maps status), raises `TransportError` on network failure + `AuthError` rather than send a blank auth, reads token **fresh per request**. Session factory (`dal/session.py`, `build_live_client`) — the one production construction site (token_provider + optional re-paste refresh + transport); `session_status` masked no-network health. Operator CLI (`dal/login.py`): paste/status/check/clear. 14 new tests (301 total). **Operator action (out of code):** capture the own-session Bearer + `login paste`; then a real multi-month forward run can accrue → promotes modules past OBSERVATION_ONLY (RULE B) → opens LD-8. |
| 11 · In-app credential + MFA login | ✅ | 2026-07-03 | Spec v1.2 §9.1 — credential login replaces Bearer-paste as the primary auth surface. `dal/auth.py` (`AuthClient`) implements the **verified** §4.1 flow: `login/v6/username` → `mfa/…/challenge/{start,otp/send,otp/verify}` (caller **loops** send→verify until `CHALLENGE_FINISH`) → `login/v6/new-device/verify` → `{access,refresh,user}`; bad-creds/failed-OTP→`AuthError`, network/5xx→`TransportError`, **never logs** password/OTP/recaptcha/tokens. `token_store` extended: access+refresh session as one JSON blob under a second Keychain account; `access_token()` prefers the login session, falls back to the pasted Bearer. `build_live_client` reads `access_token`; `build_session_refresh` seam + `AuthClient.refresh` **fail loud** until the refresh route is captured. CLI `login` subcommand drives the OTP loop; `paste` kept as fallback. `ui/login_view.py` pure state machine (CREDENTIALS→OTP→FINISH, credentials/OTP transient, never rendered/logged); `ui/app.py` auth-gates the modules + sign-out; `run.sh` drops the fail-loud precondition (login form served instead). 23 new tests (324 total). **Live-gated (operator, not code):** reCAPTCHA-v3 *enforcement* probe (token carried as a pass-through param) + refresh-route capture — §4.1 open items, not guessed. Engine/gate/weight/RULE untouched. |
| 12 · Login loop fix + DAL network-error logging | ✅ | 2026-07-03 | Two ops/observability fixes; no locked-behavior change, no bump. **(a) Login `Event loop is closed` fix** (`ui/app.py`): `_run` now drives coroutines on ONE persistent per-session event loop instead of `asyncio.run` (which opened+closed a fresh loop per submit, orphaning the `AuthClient` httpx pool cached in `session_state` → RuntimeError on the 2nd submit). **(b) Centralized network-error logging** (`dal/netlog.py`, `log_net_error`): every DAL transport seam (`HttpxTransport.get/post`, `AuthClient._post`, `ExodusClient._request`/`_maybe_backoff`) now emits one greppable `net-error` line — method + path + `status`\|`err=<ClassName>` + coarse outcome. WARNING for retryable/transient (network blip, 5xx, 429, in-flight retry), ERROR for fail-loud/exhausted (401-after-refresh, unexpected status, retries out). Redaction preserved-by-construction: the helper takes no body/token and logs the exception **class name only**, never its message (which can echo a URL). 14 new tests (337 total) incl. secrets-never-logged + no-log-on-success. |
| 12b · reCAPTCHA v3 token capture | ✅ | 2026-07-03 | Ops/observability follow-on to slice 11/12; no locked-behavior change, no bump. **Closes the §4.1 reCAPTCHA enforcement probe:** confirmed from a successful-vs-failed own-session capture that `login/v6/username` **rejects an empty token 400 "Permintaan tidak valid"** — v3 is enforced, a pure-Python login is impossible. Chosen fork = **operator-assisted token** (not a headless browser — heavy dep vs stdlib-core posture): new `dal/recaptcha.py` renders a DevTools-console snippet (`grecaptcha.execute(SITE_KEY,{action}).then(copy)`) surfaced at the paste point in CLI `login` and the Streamlit sign-in form; operator mints a fresh (single-use, ~2min) token and pastes it. Site key pinned in `config.AUTH_RECAPTCHA_SITE_KEY` (public, client-side by design) + action `"login"`. Login views **refuse an empty token before any request fires** (`LoginController.submit_credentials` + CLI). `DATA_SOURCES §4.1` + `auth.py` docstring updated: enforcement resolved; `player_id` required-vs-optional and the refresh route remain the open items. 2 new tests (empty-token guard asserts no request fires; snippet carries site key + action); full suite green (`343 passed`, incl. pre-existing uncommitted `netlog` test edits not in this commit). Engine/gate/weight/RULE untouched. |
| 12c · No-browser login (recaptcha presence-only + stable player_id) | ✅ | 2026-07-03 | Ops follow-on to 12b; no locked-behavior change, no bump. **Reverses 12b's "reCAPTCHA enforced / pure-Python impossible" conclusion via live probe against the own account:** `login/v6/username` validates only that `recaptcha_token` is **present & non-empty**, NOT its content/freshness — a reused stale HAR token AND an arbitrary junk string both log in 200; only empty/absent → `400 "Permintaan tidak valid"`. So **no browser/console/bookmarklet/headless is needed**: the client sends a fixed placeholder (`config.AUTH_RECAPTCHA_PLACEHOLDER`). **`player_id` resolved = the device-trust anchor:** required (empty → 400); a *previously-verified* UUID → trusted-device **direct session, no MFA** (new nested shape `data.login.token_data.{access,refresh}` — `_session_from_data` now parses both it and the step-5 flat shape); a *fresh* UUID → new-device MFA branch. Approach = generate a UUIDv4 **once**, persist in Keychain (`KEYCHAIN_PLAYER_ID_ACCOUNT`), reuse forever: `KeychainTokenStore.player_id()` mints-on-first-read, survives sign-out `clear()` (device stays trusted), `clear_player_id()` forces a fresh MFA. First login on a machine does the OTP loop once; every login after is direct. Removed `dal/recaptcha.py` + the console-snippet UI (CLI `login` + Streamlit form). **Verified end-to-end live**: shipped `AuthClient` (placeholder recaptcha + trusted player_id) → real access+refresh session, no browser/OTP. `DATA_SOURCES §4.1` + docstrings updated; net −1 recaptcha test, +2 (placeholder-body + player_id-lifecycle), full suite green (344). Engine/gate/weight/RULE untouched. |
| 13 · First-run auto-bootstrap ingest | ✅ | 2026-07-03 | Ops/plumbing; no locked-behavior change, no bump. First authed run with an **empty store** auto-fetches initial data: `ingest/bootstrap.py` (`bootstrap_ingest`, pure/async) resolves the universe via **SCR-0's first production wiring** (`run_scr0`, cached to `scr0_eligible` ingest-once) then ingests 90d (`DEFAULT_DAYS`, moved here; CLI re-imports) per survivor via `ingest_symbol`, emitting per-symbol progress. `ui/app.py:_maybe_bootstrap` — the UI's **first network client** (`build_live_client`, no refresher = 401 fail-loud; transport opened/closed on the slice-12 session loop via `_run`) — keys on store emptiness (session-restored launches bootstrap too), one-shot per session, `st.status` + progress bar. AuthError → Keychain cleared → login form (flag reset); other `ExodusError` → partial summary + retry button, kept symbols stay (ingest-once resume), terminal never bricked. **Live verification exposed + fixed two slice-1/2 DAL wire defects** (see decisions log): screener + OHLCV pagination enforced server-side (results were silently capped), and `marketdetectors` aggregates any multi-day range → `broker_summary` is now single-day and the pipeline pulls one broker call per missing day (bars written last as the ingest-once commit marker). Verified live end-to-end: SCR-0 → 101 eligible; per-day broker rows + full-range paginated bars land in the store; UI glue verified headless (AppTest) on both the fires-on-empty and skips-on-populated paths. `./run.sh ingest` reworded as backfill/manual fallback. 12 new tests (363 total). Engine/gate/weight/RULE untouched. |
| Portfolio auto paper-trader + market regime | ✅ | 2026-07-08 | Portfolio layer over the single-symbol runner (`validation/portfolio_runner.py`, `run_portfolio_forward`): walks the candidate universe day-by-day — **exits first** (frees name/sector room + cash), **mark + §6 circuit state** from running realised+unrealised P&L, then **entries** ranked by INTERNAL SMS (RULE B: ordering only) against a **live** `Portfolio`+`CircuitState` so the §6 10%/name & 30%/sector caps and the −3%/−10% breakers finally *bind* (the single-symbol `runner` fed them a static book, so they never clamped). Deployment is emergent (no gross-exposure target); `MAX_CONCURRENT_POSITIONS` optional count cap defaulted OFF. Reconciles with `runner.run_forward` on a one-name universe (shared fill engine, §13). Market **regime observation** (`signals/regime.py`, OBSERVATION_ONLY): RISK_ON/NEUTRAL/RISK_OFF/UNKNOWN from a look-ahead-safe proxy return series; `725bec9` extended it to **trend + breadth** — store-level `market_breadth` (advance-decline fraction, missing≠zero) + `classify_market_regime` confirms the equal-weight trend proxy with breadth (convergent Zweig Four-Percent+A-D / Murphy primary-trend+breadth); weak breadth vetoes a RISK_ON read even on a rising proxy (A-D divergence → NEUTRAL). Exposes **no allocation multiplier**, wired into nothing — scaling allocation by regime is a deferred, spec-bump-gated decision (RULE B). 13 new tests (443 → **456 total**). Engine/gate/weight/RULE untouched; no locked-behaviour change, no bump. |
| ARMED watchlist per-name Track A/B | ✅ | 2026-07-10 | Made the ARMED-watchlist rail **spec-faithful to §3 track assignment** — it was hardcoded to score *every* name as Track B (`app.py` `_all_results(store, "B", …)`), a prototype shortcut that mis-scored large-caps (broker-concentration weight 35 instead of 20, foreign-flow lead thrown away). Now each name is assigned its own §3 track (**A xor B, never blended, never A∪B**) and evaluated once under it. New shared rule `universe/track.py` (`assign_track`, `resolve_track`) is the single source of truth — `universe/gate.py` now imports it (Track enum moved there, re-exported) so gate and watchlist **cannot drift** (reconciliation-tested). Track A needs LQ45/IDX80 membership **and** ADV ≥ 25 bn; index membership — a live `emitten/{sym}/info` snapshot, previously unstored — is now persisted in a new `symbol_index` roster (`SymbolIndexRow`, keyed `(symbol, as_of)`, look-ahead-safe read-latest), populated by a `refresh_membership` pass in the bootstrap + ingest CLI (overlay, not a gate: fetch failure → Track B, 401 still fails loud). **Missing membership → Track B** (never invents A from absent data). Watchlist cache re-keyed on the roster version so a reconstitution re-tracks names. **No spec bump** (conforms to §3; the hardcoded B was the deviation). **No RULE B change** — track alters only internal SMS weighting; the rail still shows the state word + component spark-bars, never the number (A-tracked rows RULE-B-tested). No §4 weight/threshold edits. Accepted effect: correctly-A large-caps get stricter without foreign flow, so the lone ITMG B-arm drops from the rail — correct, and it doesn't touch the validation ledger. Verified end-to-end on the real store (AMMN/ADRO → A; AALI stays B on thin ADV). 15 new tests (**473 total**, full suite green). |
| 9 · Scale / ML (gated) | ✅ | 2026-07-03 | ML layer built as a **harness gated shut** (LD-8), mirroring slice 8. LD-8 admission gate (`ml/admission.py`) — the ML analogue of RULE B: reads the server-authoritative `ValidationLedger`, admits ML only when the rules system (`sms`) is VALIDATED; `require_admission` raises and is called by every ML entry point. Purged + embargoed walk-forward CV (`ml/cv.py`): anchored forward folds, label-overlap purge, boundary embargo, insufficient-samples raises. Engineered features only (`ml/features.py`) = the §4 SMS component sub-scores (no new signal). Walk-forward Sharpe weight **optimizer** (`ml/optimizer.py`) — sole writer: coordinate ascent maximizing train-fold Sharpe, worst OOS test-fold Sharpe as the accept gate, preserves the locked §4 simplex (sum=100, LD-1 Track-B `foreign_flow`=0), proposes only + never degrades. No-hand-edit weight store (`ml/weights_store.py`): no raw setter, `apply_proposal` re-checks admission + refuses non-improving; `compute_sms(weights=…)` seam scores candidates without mutating globals. Doubly-gated ML ranker (`ml/ranker.py`): LD-8 admission + RULE B display withhold. View `ui/ml_view.py` + Streamlit "⚙ ML Layer 🔒" pane surfaces the gate. 20 new tests (287 total). **Deferral (unchanged from slice 8):** the gate opens only on a real multi-month forward-paper run (live transport + accrued time); until then the layer stays LOCKED by design. |

---

## Module validation state (RULE B)

State machine: `OBSERVATION_ONLY` → (accrues forward-paper) → `VALIDATING` → (≥ `PAPER_VALIDATION_MONTHS`
positive walk-forward) → `VALIDATED`. Only `VALIDATED` modules may show a number.

### Ships-now — observation modules (no gate; never show a predictive number)
| Module | State | Notes |
|---|---|---|
| Broker Flow Analyzer | ✅ built (2026-07-02) | Pure observation — net flow, DNA, top-N/HHI, persistence, syndicates, matrix. No score, no verb. |
| Foreign Flow Dashboard | ✅ built (2026-07-02) | Pure observation — NBSA series, persistence, reversal, splits, KSEI overlay. Multiples/z-scores are measurements, no score, no verb. |
| Institutional Accumulation Detector | ✅ built (2026-07-02) | Pure observation — stealth divergence, accumulator VWAP, volume dry-up + price tightness. Absorption needs L2 depth → `None` (graceful). No score. |
| Money Flow Replay | ✅ built (2026-07-02) | Audit tool — every frame re-read from the store at its historical `decision_ts`; reconciles with live signals (acceptance-tested). Wyckoff phase lane now lit by the slice-4 classifier (label only, `UNKNOWN` until enough history). |
| Smart Money Heatmap | ✅ built (2026-07-02) | Derived visualization — direction + intensity (flow-as-%-of-cap), sector drill-down, local-buy/foreign-sell divergence alerts. No score. |
| Distribution / Trap layer | ✅ built (2026-07-02) | Pure observation (slice 5) — §8 signal-decay flags: PHASE_ROLLOVER, NO_DEMAND, BEARISH_DIVERGENCE, FOREIGN_OUTFLOW. `TrapMonitor` unifies §5 veto traps + §8 decay; ribbon wired into every view. Categorical severities (INFO/WATCH/WARN), no number. |
| Sector Rotation Map | ✅ built (2026-07-02) | Derived visualization (slice 6) — net-foreign flow by sector on the RS-vs-flow quadrant (spec §9 labels), RS vs universe proxy, tide. RS/flow are measurements; the quadrant is a categorical observation. No score, no verb. |
| Portfolio Risk Monitor | ✅ built (2026-07-02) | Risk observation, not return prediction (slice 6) — §6 exposure caps, sector HHI, same-bandar crowding + correlated-pair check, β vs injected benchmark, historical VaR, days-to-exit, scenario stress (what-ifs), §6 circuit breakers. VaR/β/HHI are risk measurements; scenarios are hypothetical, never forecasts. No score, no verb. |
| Market Regime | ✅ built (2026-07-08) | Pure observation — RISK_ON/NEUTRAL/RISK_OFF/UNKNOWN from a look-ahead-safe equal-weight trend proxy **confirmed by advance-decline breadth** (Zweig/Murphy convergence); weak breadth downgrades a rising proxy to NEUTRAL (A-D divergence). Trend %/breadth are measurements; the regime is a categorical label. **No allocation multiplier — wired into nothing.** Promoting it to a gross-exposure input needs a spec LD bump + forward-paper validation (RULE B). |

### Gated modules — NO number until earned (RULE B)
| Module | Validation state | Paper-months accrued | Notes |
|---|---|---|---|
| Smart Money Score (SMS) | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 4): components shown as raw observation, internal 0–100 drives ARMED, **number withheld (`•••`)** by `validation.state` until VALIDATED. Slice 8: promotion now flows through the server-authoritative `ValidationLedger` (`validation.promotion`) — the sole writer; state stays OBSERVATION_ONLY until a real forward-paper run clears ≥3mo + positive walk-forward Sharpe. RULE-B-tested end-to-end. |
| AI Buy/Sell Ranking | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 8): `ui/ranking_view` — names ordered by internal flow strength ("flow-derived ranking, not a recommendation"); position + score **withheld (`•••`)** until VALIDATED via the ledger. Also gated by LD-8. |
| Daily Top Opportunities | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 8): `ui/daily_top_view` — "highest flow-signal names today," ARMED names surfaced with SMS *components* as observation; composite score **withheld** until VALIDATED. |

---

## Validation metrics log

Record forward-paper results here as they accrue (net of full fee stack, benchmarked to LQ45/sector).

| Date | Module | Ann. return (net) | Sharpe | Max DD | Hit rate | Turnover | vs benchmark |
|---|---|---|---|---|---|---|---|
| — | — | — | — | — | — | — | — |

---

## Decisions & deviations log

Record any spec deviation here with a reason and the spec version bump it triggered.

| Date | Change | Reason | Spec version |
|---|---|---|---|
| 2026-07-08 | **§4 SMS component calibration fixed — the `ARMED@70` deadlock.** A full-year backtest over 32 liquid names (ingested 1yr history) surfaced that the internal SMS **never reached 70** (max 62.2 across 5,760 evals; p50 ≈ 35), so no module could ever accrue forward paper → RULE B could never promote → the whole validation path was blocked. Per-component funnel isolated the cause: **divergence** (weight 30, the spine) sat pinned near 0 (sub-score p90 0.11, max 0.41) and **block_trade** was pinned at 1.0 for everyone. Two scoring-shape defects fixed (`signals/sms.py` + `config.py`): **(a)** divergence was computed over the *whole* passed-in history → its flat-high-volume ratio diluted to ~0 for anything that trended; now over a **recent window** (`SMS_DIVERGENCE_WINDOW_DAYS=40`) with a **graduated** corr factor `_corr_factor` in [0.5,1.0] (reduces to the old binary rule at corr 0 and 0.3) replacing the ×0.5 cliff that fired on nearly every name (year-long vol/\|move\| corr ≈ 0.5). **(b)** block_trade's fixed IDR-1B floor OR'd with the %ADV test **saturated to 1.0** on any liquid name (a 1B buy is noise for a large-cap); now graded by max single-broker buy as a **fraction of ADV** (§4's "> 1% ADV"), IDR floor kept only as the ADV-unknown fallback. **RVOL investigated, left unchanged** — near-zero on quiet accumulation days is spec-faithful (it is the volume-*anomaly* component; low readings are correct). **§4 conditions (±0.5%, corr 0.3 reference), all six weights, and `ARMED@70` are untouched — no weight was hand-edited, no threshold moved.** Approach was **defect-fix only, explicitly NOT tuning to manufacture trades** (RULE B). Evidence post-fix (**in-sample backtest, does NOT promote**): max SMS 62→72; **1 name (ITMG) armed → valid trigger → round-tripped** 2026-06-10→11, net −286,910 IDR (−0.29%, SIGNAL_DECAY) — proving the full vertical (engine→trigger→order→shared fill→exit→net-of-fee `PaperTrade`) runs on real data. Arming stays **rare** (1 trade/yr/32 names — too sparse to forward-validate yet; walk-forward needs ≥6 trades); vetoes remain a tight secondary constraint (542/886 C/D vetoed). 3 new tests (456 → **459 total**). | The composite could not reach its own locked threshold on real data — a self-inflicted deadlock blocking every gated module. These are scoring-normalization corrections (how a component maps to [0,1]), the documented tunable surface, not weight/threshold edits; the fix was kept principled (recency, scale-relative, graduated) rather than fished to a trade count. Warrants a documented bump per CLAUDE.md since divergence's corr handling and block_trade's scoring shape are §4 behavior. | **v1.3 (bump)** |
| 2026-07-01 | Initial companion files created | Spec §14 requires PLAN/CLAUDE/PROGRESS | v1.1 (no bump) |
| 2026-07-01 | UI design handoff added under `design/` | Operator-provided hifi design target for §9 modules | v1.1 (no bump) |
| 2026-07-01 | Front-end stack = Streamlit (chose over React/hybrid) | Stay faithful to spec §10; single-user local tool; accept approximate fidelity | v1.1 (no bump) |
| 2026-07-01 | Store pinned = DuckDB (chose over SQLite) | Analytical (OLAP) workload — backtest, window-function aggregations (persistence/Herfindahl/NBSA), Replay range-scans over `(symbol, date, as_of)`; native Pandas/Polars/Arrow integration; nightly-batch single-writer ingest makes SQLite's OLTP/concurrency edge irrelevant. Within spec §10's SQLite/DuckDB allowance — pin, not a deviation. | v1.1 (no bump) |
| 2026-07-01 | Slice 1 impl choices: async DAL (injectable transport); timestamps WIB-local tz-naive; broker `as_of` = feed `data_last_updated` else conservative next-day 09:00 (LD-5) | Async matches DATA_SOURCES §6 surface + enables throttled concurrent paywalled pulls; single-exchange tz needs no zone mixing; conservative fallback keeps look-ahead honest until latency measured. Implementation detail within spec. | v1.1 (no bump) |
| 2026-07-02 | Dev-board ARA/ARB band (spec's "±10–25%" range) resolved by price tier: prev close ≥ 5000 → 10%, else 25%; ε = 0.5% absorbs tick rounding at the band; unknown board falls back to the tightest (main 7%) band | Spec §12 pins a range, not a rule; tiering mirrors IDX's tighter-band-for-higher-price convention; conservative fallback never hides a pinned close. Constants in `config.py`, tunable when measured against real ARA/ARB days. | v1.1 (no bump) |
| 2026-07-02 | Broker-DNA registry seeded from design handoff (KZ/AK/RX/ZP/YU foreign-inst, CC/NI/OD/DR local-inst, DX/AI/KI smart-money, BQ prop, YP/PD/CP/GR retail); fallback to feed's Asing/Lokal tag; unmapped local codes stay UNKNOWN | No served DNA feed; registry is operator knowledge, explicitly illustrative and overridable per call — never silently guessed. | v1.1 (no bump) |
| 2026-07-02 | Rebalance down-weight fires only when ALL of: β-residual ≤ 1%, tracker-broker flow share ≥ 50%, within ±7d of an MSCI review date (operator-maintained calendar in `universe/rebalance.py`) | §3 says down-weight pure-beta moves, not every move near a rebalance; conjunction keeps genuine alpha at full weight. Thresholds in `config.py`. | v1.1 (no bump) |
| 2026-07-02 | Replay frame for trading day D reconstructs `decision_ts` = D+1 09:15 WIB (`config.REPLAY_DECISION_TIME`, injectable) | First actionable pre-open moment at which both D's EOD bar (~16:15) and D's broker summary (LD-5 conservative D+1 09:00) are knowable; earlier moments would show broker lanes always empty. Revisit once publish latency measured. | v1.1 (no bump) |
| 2026-07-02 | KSEI ownership `as_of` = fetch time (not month-end) | KSEI publishes monthly with an undisclosed lag; stamping month-end would fabricate availability. Fetch time is the only honest availability claim — conservative by construction. | v1.1 (no bump) |
| 2026-07-02 | Foreign-vs-domestic "split" rendered as participation (turnover share) + signed nets; NBSA vs-20d multiple uses mean |net| (magnitude-robust) alongside a signed z-score | Per-stock net domestic ≡ −net foreign (two sides per trade), so a net-only split is a mirror; a signed 20d mean near zero makes the raw ratio explode. Both stats are measurements (RULE-B-safe), not scores. | v1.1 (no bump) |
| 2026-07-02 | Wyckoff classifier is **detector-fed & heuristic**: selling-climax/AR anchors the trading range (else a consolidation-base fallback); spring→C, SOS+LPS→D, UTAD/weak-up-bars→DISTRIBUTION, extension→E; thresholds in `config` (`PHASE_*`). SOS without a confirming LPS stays non-tradeable (B). | §2/LD-2 pins the *gate* (only C/D tradeable) but not an algorithm. Conservative-by-default (ambiguous → not tradeable) honors RULE A; thresholds are tunable against labeled real charts as the DNA registry is. | v1.1 (no bump) |
| 2026-07-02 | SMS components each normalize to a sub-score in [0,1]; SMS = Σ weightᵢ·subscoreᵢ × rebalance multiplier. Weights live in `config.SMS_WEIGHTS` (the only tunable surface). | §4 pins weights + the ARMED@70 threshold but not each detector's shape; normalization keeps the composite in 0–100 and each component independently observable (RULE B components). Weights isolated so only the walk-forward optimizer writes them. | v1.1 (no bump) |
| 2026-07-02 | Markup-on-thin-volume veto fires only on **up** spikes (pump), not the spring's downward shakeout | §5 defines it as "price spiking on low value traded (pump signature)" — markup is upward. Using `abs(Δprice)` mis-vetoed the Phase C spring. Fix keeps the C event intact. | v1.1 (no bump) |
| 2026-07-02 | RULE B switch: `validation.state` (per-module state, default OBSERVATION_ONLY) is the server-authoritative gate; `ui/sms_view.score_display` returns `•••` until VALIDATED. Slice 8 promotes via the paper-trade engine. | LD-9 requires the observation↔claim switch to be server-authoritative, never a client toggle. Shipping the switch now (defaulted closed) satisfies §13's RULE B acceptance test; slice 8 only flips it. | v1.1 (no bump) |
| 2026-07-02 | Replay Wyckoff phase lane now carries the classifier **label** (RULE A verdict, not a number), read over a longer look-ahead-safe base (`REPLAY_PHASE_LOOKBACK_DAYS`); `UNKNOWN` when history is short | The lane was a placeholder pending the slice-4 classifier, which now exists. A label is a gate verdict, not a score — RULE-B-safe; longer base is needed for a range to form and stays `as_of`-filtered. | v1.1 (no bump) |
| 2026-07-02 | Slice 5 distribution layer = the **exit/decay** complement to slice-4 vetoes, not a re-implementation. Veto (§5) rejects an *entry candidate*; a decay flag (§8) warns on an *open/ARMED* name. New detectors: PHASE_ROLLOVER, NO_DEMAND (VSA), BEARISH_DIVERGENCE, FOREIGN_OUTFLOW. UTAD/distribution & dominant-broker-flip stay in the phase classifier / veto and are *surfaced* (not duplicated) via `TrapMonitor`. Flags are categorical severities (INFO/WATCH/WARN), never numbers (RULE B). Thresholds in `config.DECAY_*`, tunable against labeled charts. | v1.1 (no bump) |
| 2026-07-02 | SCR-EXIT scans IHSG server-side (per screeners.md); `exit_flags_for` intersects survivors with the operator's open+ARMED watchlist so "runs continuously over open + ARMED names" (spec §11) holds without a bespoke per-name screener call — off-watch survivors are logged, never silently dropped. | v1.1 (no bump) |
| 2026-07-02 | Slice 6 **Sector Rotation Map**: quadrant = sign pair of (relative strength, net-foreign flow) → LEADERS / EARLY_RECOVERY / DISTRIBUTION_WARN / AVOID (spec §9's own labels; the ≥0 side wins ties, no dead zone). Relative strength = sector mean return − universe equal-weight mean return (a **market proxy**, since no IHSG/index feed is ingested and §8 forbids IHSG as a *return benchmark* — here it only frames the RS axis). RS/flow are measurements, the quadrant a categorical observation (RULE B). A sector missing either axis carries `quadrant=None` (missing ≠ zero). Window in `config.SECTOR_WINDOW_DAYS`. | v1.1 (no bump) |
| 2026-07-02 | Slice 6 **Portfolio Risk Monitor** is observation-only per §9 ("risk observations, not return predictions"): VaR/β/HHI/crowding-ρ/days-to-exit/what-if impacts are risk **measurements**, never a confidence, probability, SMS, or buy/sell verb (RULE B-safe by tier). Scenario stress rows are defined hypothetical shocks (config-pinned), never forecasts. | v1.1 (no bump) |
| 2026-07-02 | **β vs benchmark is injected, not IHSG-fed** — no index (IHSG) DAL feed exists, so `build_risk_report` takes `benchmark_returns`; β is `None` without it (missing ≠ zero). `market_proxy_returns` (universe equal-weight mean) is offered as an honest labeled proxy and is what the UI passes; wire a real IHSG series when a feed lands. Design's "β vs IHSG" is a systematic-risk measurement, distinct from the §8 rule that returns are benchmarked to LQ45/sector — not a conflict. | v1.1 (no bump) |
| 2026-07-02 | **"Same-bandar" crowding = cosine similarity of two names' net-by-broker vectors** (from the look-ahead-safe broker-flow snapshot); the correlated-pair check (§6) flags pairs with ρ ≥ `CROWDING_CORR_THRESHOLD` (0.70) and names the shared lead broker. A cosine captures "the same syndicate drives both on the same side" (design's DX-shared example) directly from stored flow; the §6 exposure caps (10% name / 30% sector) and §6 circuit breakers (−3% daily / −10% drawdown) are pure functions over supplied portfolio + injected P&L. Positions are an **input** — the IDX fill engine that produces them is slice 7; P&L stays `None` until an entry price exists, so no fabricated return is shown. | v1.1 (no bump) |
| 2026-07-02 | **2-yr backtest deferred from slice 4 to slice 7** | §11/§13 require backtest and forward-paper to *share one fill engine* and report net-of-full-fee-stack returns; the IDX fill engine lands in slice 7. Running a P&L backtest now (no fills/fees/exits) would violate that discipline and produce a number the spec forbids. Engine + phase are already look-ahead-safe and replay-auditable in the interim. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **fee stack** modelled as four explicit auditable components: broker commission (side-specific 0.15% buy / 0.25% sell, honouring §12's "~0.15–0.25%" range), IDX/KPEI/KSEI levy (~0.043%, both sides), PPN VAT 11% on the commission, and the 0.1% final sales tax on the sell notional only. | §12 lists the stack as "broker + levy + VAT + 0.1% sell tax"; decomposing each line makes the §13 hand-checked cases pin every component and keeps the "full fee stack" auditable. Rates in `config.FEE_*`, tunable to the operator's actual broker schedule. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **tick bands** = current IDX fraksi-harga regime (<200→1, 200–<500→2, 500–<2000→5, 2000–<5000→10, ≥5000→25); **slippage** tiered by ADV (≥100bn LARGE 0.1% / ≥25bn MID 0.35% / else SMALL 1.2%, midpoints of §12's LQ45/mid/small ranges), buys slip up & sells slip down, unknown ADV → SMALL (most conservative). | §12 pins lots/bands/slippage ranges but not exact ticks or the tier cutoffs; the fraksi table is the live IDX regime and midpoint slippage is the honest central estimate. Constants in `config.TICK_BANDS`/`SLIPPAGE_*`, tunable when measured against real fills. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **fill semantics**: next-open + adverse slippage, rounded to a valid tick, then **limit discipline** — a buy above its limit / a sell below it is an honest NO_FILL, and a fill slipped past the limit is capped at the limit (never exceeds it). ARA/ARB rejects only the **adverse** side (a buy into a locked limit-up = no offers; a sell into a locked limit-down = no bids); a buy into a limit-down is allowed. | LD-3: next-open + ARA/ARB make market fills fiction, so the engine only fills limits and never invents an execution. Modelling the adverse-side lock and the limit cap keeps paper fills fill-realistic (the §13 discipline the whole validation gate rests on). | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **trigger geometry**: Phase C spring → entry = spring-bar close, stop = spring low − `STOP_BUFFER`, target = range resistance (AR high); Phase D LPS → entry = LPS close, stop = LPS low − buffer, target = resistance + one range span (a Wyckoff measured move, since price already reached resistance in D). R:R computed on the actual entry limit; < 2:1 → SKIP. | §6 pins "close of spring-test / LPS pullback" as the trigger and "AR high / next HVN" as the first target with R:R ≥ 2:1, but not the exact levels; anchoring on the classifier's own range + event bars keeps the geometry structural and replay-auditable. Buffer/target-mult in `config`, tunable. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **exit priority** is capital-first: STOP → TARGET → TRAILING → SIGNAL_DECAY, evaluated on the latest bar (daily cadence re-evaluates each day); a bar spanning both stop and target assumes the worst (STOP). Any active §8 decay flag triggers a SIGNAL_DECAY exit (the decay detectors already encode §8's exit conditions), with the most-severe flag's detail surfaced. | §8 lists the exit triggers but not their precedence; protecting capital first is the conservative ordering, and reusing `signals.distribution` avoids re-implementing the §8 decay logic (divergence = the single best exit signal). | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **`PaperTrade` P&L is engine-derived, not recomputed** — net P&L = the two shared-engine fills' signed `cash_flow` sum (each already net of the §12 fee stack). No fee math is redone in `validation/`. | Makes "net of full fee stack" true by construction and guarantees backtest ⇄ forward reconcile (both build trades from the same fills); a second fee model would risk divergence the §13 reconciliation test exists to forbid. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **backtest ⇄ forward = two entry points, one engine**: `run_backtest` sweeps the window in a batch loop, `run_forward` steps day-by-day (flat→seek entry / holding→manage exit); both delegate to the same `_attempt_entry`/`_attempt_exit` (hence the same `paper.fill.fill_order` + `execution.risk`). Exit = market-style next-open sell modelled as a sell-limit at a sub-quote floor (always fills at next open net of adverse slippage; a genuinely locked ARB still REJECTS and carries a day). Per-day `decision_ts = combine(day, REPLAY_DECISION_TIME)` keeps every decision look-ahead-safe; the fill uses the day's known open. | §11/§13 pin "separate code paths sharing one fill engine; results reconcile" but not the loop shape; two real loops over shared helpers satisfy both the reconciliation invariant and the honest live-accrual vs batch distinction. Exit-as-limit-floor keeps the §12 next-open discipline without inventing a market fill (LD-3). | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **walk-forward Sharpe = worst of N sequential out-of-sample folds** (`folds=3`, min fold Sharpe; per-trade mean/σ, no annualisation — sign is what promotion reads); `None` when < 2·folds trades or any fold has zero dispersion. "Positive walk-forward" ⇒ every fold was risk-adjusted-positive, not just the aggregate. | §8 mandates walk-forward + out-of-sample and forbids a headline number until earned; requiring every fold positive is the conservative reading and resists a single lucky stretch carrying the record. Fold count tunable. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **promotion gate** (`validation.promotion.resolve_state`): OBSERVATION_ONLY when months=0; VALIDATED only when months ≥ `PAPER_VALIDATION_MONTHS` **and** walk-forward Sharpe > 0; otherwise VALIDATING (incl. enough months but non-positive/absent walk-forward). `ValidationLedger.record_forward_paper` is the ONLY writer — no public setter marks a module VALIDATED, so the observation↔claim switch stays server-authoritative (LD-9), never a client toggle. | §8's validation gate + LD-9's "paper-trade engine is the sole authority"; making time-accrued-but-unprofitable stay VALIDATING (never VALIDATED) prevents a module earning its number on tenure alone. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **IHSG benchmark refused at the metrics boundary**: `compute_metrics` raises on benchmark ∈ {IHSG, JCI, COMPOSITE, IDX COMPOSITE, ^JKSE}; Track A → LQ45, Track B → sector. | §8 "never IHSG as headline benchmark" — enforcing it where the number is produced makes an accidental composite benchmark impossible, not merely discouraged. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **`gated_display(module, value, registry, fmt)`** is the single observation↔claim primitive for every gated module (SMS/AI-ranking/Daily-Top); returns `•••` unless the module is VALIDATED (and always for a `None` value). `ui/sms_view` refactored onto it; ranking/daily-top view-models added; the Streamlit app reads `ValidationLedger.states()` (server-authoritative). | §13 requires the switch across *all* gated modules, not just SMS; one primitive keeps the withhold rule identical everywhere and removes the per-view WITHHELD duplication. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **ML layer built as a harness gated shut** (LD-8), mirroring slice 8's promotion harness. An admission gate (`ml/admission.py`) reads the server-authoritative `ValidationLedger` and admits ML only when the rules system (`sms`) is VALIDATED (≥`PAPER_VALIDATION_MONTHS` + positive walk-forward Sharpe). `require_admission` raises `MLNotAdmittedError` and is the first call in every ML entry point (optimizer, ranker, weight-apply). In production nothing has cleared forward paper, so the whole layer refuses to run — the honest state until a real forward run lands. | LD-8 defers ML until the rules demonstrably survive forward paper; making admission a server-authoritative read of the same ledger (never a client flag) is the ML analogue of RULE B, and shipping it closed-by-default satisfies "gated" without a live run. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **purged + embargoed walk-forward CV** (`ml/cv.py`): anchored forward folds (train strictly precedes test — the only honest OOS direction for a live-accruing strategy), **purge** of any train sample whose label span [t0,t1] overlaps the test window, **embargo** = `ceil(embargo_frac·n)` boundary samples dropped; unresolved labels (`t1=None`) treated as far-future ⇒ conservatively purged; too-few-samples raises `InsufficientSamplesError` (missing ≠ zero). | LD-8 mandates purged/embargoed CV + out-of-sample; anchored walk-forward + label-overlap purge + boundary embargo is the López de Prado discipline reduced to deterministic index math (no numpy/sklearn — the core stays stdlib per §10). | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **optimizer is the sole weight writer**: deterministic coordinate ascent on the integer weight simplex maximizing mean **train-fold** Sharpe; the **worst OOS test-fold** walk-forward Sharpe is the accept gate. Locked §4 structure preserved (weights sum to `ML_WEIGHT_SUM`=100, `ML_LOCKED_ZEROS` — Track-B `foreign_flow` — never funded). It **proposes only**; `improved` iff OOS>0 AND ≥ incumbent OOS AND changed. `compute_sms` gained a `weights=` override so candidates are scored without mutating `config.SMS_WEIGHTS`. | §4/CLAUDE.md: weights are the only tunable surface and only via walk-forward Sharpe — never hand-edited live. Search-fits-on-train / gate-on-OOS resists overfitting; proposing-not-mutating + the compute_sms seam make hand-editing structurally impossible. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **no-hand-edit weight store** (`ml/weights_store.py`): a provenance-tracked surface with **no raw setter** — the only mutation is `apply_proposal(proposal, ledger)`, which re-checks LD-8 admission and refuses any non-`improved` (degrading) proposal, recording provenance on every applied change. | "No live hand-editing of SMS weights" (§13 acceptance) is enforced by construction — there is no code path that accepts raw weights, and application is re-gated so a proposal existing never implies it may go live. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **ML ranker is doubly gated** (`ml/ranker.py`): a transparent linear ranker over engineered features, refusing to run without LD-8 admission AND withholding its score/position (`•••`) until the `ai_ranking` module is itself VALIDATED (RULE B). Ordering is observation ("flow-derived ML ranking"), the number is a claim. | §9/LD-8: ML also governs the AI ranker. Two independent server-authoritative gates (LD-8 admits the *layer* to run; RULE B gates whether *that module* may show a number) keep both invariants honest without conflating them. | v1.1 (no bump) |
| 2026-07-03 | Slice 10 **live DAL transport = paste-token + macOS Keychain** (not a spec §11 slice; closes the standing deferral). Bearer captured from the operator's own authenticated session and stored via the `security` CLI (subprocess, zero new dep — stdlib core per §10/slice-9 posture); httpx transport binds onto the client's already-injected `Transport`/`PostTransport` seam (returns raw `Response` so the client keeps mapping status codes; raises `TransportError`/`AuthError` per the contract; token read fresh per request so refresh needs no rebuild). `build_live_client` is the sole production construction site. | The client was transport-agnostic by design since slice 1; this only supplies the prod transport DATA_SOURCES §6 always assumed — no locked behavior changes. Keychain over a file honors CLAUDE.md local-first ("nothing leaves the machine, never republished") and avoids a plaintext on-disk secret; `security` over `keyring` avoids a new dependency. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **fundamental tilt injected, not DAL-fed** — no live-fundamentals feed is ingested yet (`fundamentals_live` is a future wire), so `classify_tilt` is pure over supplied MF rank / EV-EBIT / ROE (SCR-4 rows or injected values); missing MF rank → NEUTRAL (un-tilted default, missing ≠ zero), never bottom tercile. Fundamentals never block entry (LD-6) — the tilt only sizes conviction and hold horizon. | Mirrors the slice-6 injected-benchmark posture; keeps the tilt honest and testable now, and slice 8 / a fundamentals DAL feed can supply real point-in-time values (DATA_SOURCES §3.1) without changing the interface. | v1.1 (no bump) |
| 2026-07-03 | **Auth re-scoped: in-app username/password + MFA login flow** (planned slice 11) replaces the slice-10 out-of-band Bearer *paste* as the primary auth surface. Spec §9.1 rewritten; §10/§15 updated for credential handling (creds/OTP transient in-memory, never persisted/logged; only access+refresh tokens → Keychain). Wire contract **verified** from `login-stockbit.har` and pinned in `DATA_SOURCES §4.1`: `login/v6/username` → `mfa/verification/v1/challenge/{start,otp/send,otp/verify}` (verify loops until `CHALLENGE_FINISH`) → `login/v6/new-device/verify` → `{access ~24h, refresh ~7d}`. **Not yet built** — two open items gate code: (a) `recaptcha_token` is reCAPTCHA-**v3** (invisible — no challenge shown, only OTP; token silently browser-minted) — **probe server enforcement first** (login with token omitted/junk); pure-Python login works if unenforced, else headless-browser/assisted/paste fork; (b) refresh-endpoint route/shape not in the capture. Slice-10 paste remains the working fallback until settled. | Operator asked to sign in with credentials, not hand-capture a Bearer. Still own-session, local-only, nothing republished (LD-10 holds); no engine/gate/weight/RULE change — auth plumbing only. Reverses the explicit slice-10 "not a credential/MFA login" scope, so it warrants a documented bump per CLAUDE.md. | **v1.2 (bump)** |
| 2026-07-03 | Slice 12c **— live probe overturns the 12b reCAPTCHA conclusion; login is now no-browser.** Probing the own account showed `login/v6/username` validates `recaptcha_token` **presence only** (junk + reused-stale tokens log in 200; empty → 400), so the operator-assisted console-snippet fork (12b) was unnecessary — replaced by a fixed placeholder (`config.AUTH_RECAPTCHA_PLACEHOLDER`), `dal/recaptcha.py` + its UI removed. `player_id` resolved as the **device-trust anchor**: required, and a persisted UUID (minted once, `KEYCHAIN_PLAYER_ID_ACCOUNT`, survives sign-out) yields a trusted-device **direct session (no MFA)** on repeat logins; the new nested response shape `data.login.token_data.*` is now parsed. Verified end-to-end against the live endpoint. | Server-side reality beats the earlier inference: with no `siteverify` enforced, a pure-Python login is possible and the heavy/manual browser paths are pure cost. A stable local device id is the minimal way to keep the OTP a **one-time** event. Own-session, local-only, nothing republished (LD-10 holds); auth plumbing only — no engine/gate/weight/RULE change. | v1.2 (no bump) |
| 2026-07-03 | Slice 13 **first-run auto-bootstrap = SCR-0 universe + 90d ingest, fully automatic** (operator-chosen over a typed watchlist / confirm-first). `trading_day` for the `scr0_eligible` cache row = last-weekday proxy (the screener template carries no date — it runs "as of now" server-side, so the day is a storage key only). Error split: `AuthError` fails loud → Keychain session cleared → login form (the sign-out mechanism; persisted `player_id` keeps the device trusted, so re-login stays MFA-free); any other `ExodusError` (incl. paywall/rate-limit on what is the app's largest single pull, ~100–150 names × 2 requests) is **captured into a partial `BootstrapSummary`** — kept symbols stay stored, a retry button resumes via ingest-once, the modules always render. Sequential by design (pipeline pacing); `now` stamped once per run for consistent `as_of`. | v1.2 (no bump) |
| 2026-07-03 | Slice 13 **live probes corrected two pinned feed contracts** (DATA_SOURCES/screeners.md updated): (a) `screener/templates` REQUIRES an integer `page` (omitting → 400) with `limit` as page size — `run_screener` now sends `page`+`limit` (`SCREENER_PAGE_LIMIT` 900) and pages by `totalrows`, never a silent truncation; (b) `company-price-feed/historical/summary` caps un-paginated calls at ~12 rows regardless of range and rejects `limit`>50 — `ohlcv_foreign` pages (`OHLCV_PAGE_LIMIT` 50) until a short page; (c) `marketdetectors/{sym}` returns a **range AGGREGATE stamped `netbs_date=from` for any multi-day span** — per-day rows exist only day-by-day, so `broker_summary(sym, day)` is now single-day and `ingest_symbol` pulls one broker call per missing day, writing bars LAST as the ingest-once commit marker (deterministic `as_of` makes retry re-writes exact-key no-ops — no permanent broker holes). Cost model unchanged in kind (DATA_SOURCES §7 always budgeted per-day broker backfills); a 90d bootstrap ≈ 60 paywall-counted broker calls per name. Pre-fix multi-day broker ingests stored mis-dated aggregates — the only affected data (today's BBCA smoke rows) was purged with the db; re-ingest is clean. | v1.2 (no bump) |
| 2026-07-03 | Slice 11 **landed as a harness against the verified §4.1 contract** (injected-transport tests, no live network — the slice-8/9/10 posture). `AuthClient` posts unauthenticated JSON (no Bearer yet); the caller drives the send→verify OTP loop until `CHALLENGE_FINISH`. Two §4.1 open items are **carried, not guessed**: (a) `recaptcha_token` is a pass-through param — empty for the pure-Python attempt, an operator-minted token if the enforcement probe (still an operator action) shows it's required; (b) `config.AUTH_REFRESH_PATH=None` ⇒ `AuthClient.refresh` and the `build_session_refresh` seam **fail loud** (→ re-login), never a stale/empty fallback, until a real refresh exchange is captured. Session stored as access+refresh JSON under a second Keychain account; `access_token()` prefers it and falls back to the slice-10 pasted Bearer, so both auth paths coexist and the transport is unchanged. Login view-model is pure/tested; credentials/OTP/recaptcha never persisted, rendered back, or logged (asserted). | The verified contract is fully codeable now; the two unconfirmed items require live network + a real capture (operator, not CI), so they stay deferred exactly as slice-10's real forward run does. No LD/weight/gate/RULE change — auth plumbing only; gated modules stay server-authoritative via the ledger. | v1.2 (no further bump) |
| 2026-07-03 | **Design-fidelity pass against `design/` (audit-driven; UI layer only, no locked-behavior change):** (a) design tokens land as `.streamlit/config.toml` (bg `#070a10`/`#0d121b`, accent `#58c4dd`, sidebar `#0a0e14`, DNA/semantic chart palette, Geist/Geist Mono **when locally installed** — no runtime font fetch, local-first) and `run.sh` drops its `--theme.base` flag; (b) **ARMED watchlist rail** (`ui/watchlist_view.py`, sidebar-adapted): state WORD + the design's five spark strengths `DIV BRK FF RVOL BLK` — composite number/rank/verbs never emitted (RULE-B-tested), gate-rejected/vetoed names never listed, cap reported via `dropped` (no silent caps), one cached engine pass keyed on day+symbol-count; (c) **accumulation chart** (`accumulation_view.chart_rows`): price lane + accumulator-VWAP reference + cumulative smart-money lane, broker-less days render as a **gap, never zero**; raw-dict dump replaced with the design's "measured, not scored" stealth-metrics row; (d) **login State C + OTP footer**: in-app Bearer-paste card (`login_view.submit_bearer` + `dal/session.verify_bearer`) verifies with a live ping **before** storing — a rejected token is never written; OTP round gains channel select + resend (the idle `LoginController.send_otp`) with the server's `next_attempt_in` cooldown. Deliberately NOT chased (per the 2026-07-01 approximate-fidelity decision): custom top bar/nav rail/ticker chrome, replay autoplay, pixel-exact spark bars. 13 new tests (376 total); terminal + login states verified headless (AppTest) against a copy of the live store. | The design handoff is the pinned UI target; these were the audit's highest value-per-effort gaps *within* Streamlit's native reach — closing them without a React/JS layer keeps the §10 stack decision intact while restoring the design's tokens, rail, chart, and auth affordances. | v1.2 (no bump) |
| 2026-07-04 | **VectorLab bundle updated with pixel targets, and the shell chrome implemented against them.** The operator's refreshed `VectorLab.zip` adds `design/screens/` (ten 1280×800 captures — 2 login states + the 8 terminal modules) + `design/SCREENS_INDEX.md`, declared "the pixel-fidelity targets"; specs/prototype diffed byte-identical, and the zip's `PLAN.md`/`DATA_SOURCES.md` were again stale snapshots (pre-live-probe) and NOT adopted. The chrome the 2026-07-03 pass deliberately deferred is now shipped as **pure HTML/CSS view helpers** (`ui/shell.py` — tokens inlined, no Streamlit import, no React/JS layer, §10 intact): 52px top status bar (as-of stamp from the store's max ingested day, RULE-B pill, track chip, masked operator — no IHSG quote: the feed isn't ingested and a missing datum renders absent, never faked), module-header badge pills (green OBSERVATION / amber GATED / cyan DERIVED, ledger-driven on SMS), the **right-hand ARMED-watchlist rail** (moved out of the sidebar into a main/rail column split; five spark-bars DIV BRK FF RVOL BLK with exact values only in hover tooltips, missing component = empty slot "not available", cap note never silent), the amber **per-module validation-state bar** on SMS ("n.n / 3 months — number withheld" until the server-authoritative ledger says VALIDATED), and the 26px scrolling **§15 disclaimer ticker** (all five spec lines). RULE B by construction: no composite number, rank, probability, or buy/sell verb anywhere in shell output (tested). 15 new tests (391 total); all 11 modules + login re-verified headless (AppTest) against a copy of the live store. | The screens are the declared build target; the deferred chrome was the visible gap, and HTML string-builders keep it testable and inside Streamlit's model. | v1.2 (no bump) |
| 2026-07-04/05 | **Full module-screen fidelity pass against `design/screens/` (UI layer only, no locked-behavior change; 3 commits).** (a) **Chrome overrides + Broker Flow screen** (`fa11670`): hide Streamlit header/toolbar, restyle the sidebar radio into the design's nav rail; Broker Flow rendered with the design's stock header, HTML broker table (DNA chips, signed nets with under-bars, persistence dots), concentration panel, and the previously-missing §5 veto-checks panel; broker × stock matrix scoped to the (≤7) watchlist columns with green/red intensity cells — cap annotated, missing cells absent (never zero). (b) **Rail-card selection + styled module screens** (`7670784`): the ARMED-watchlist rail cards become the terminal's symbol selector (invisible full-card overlay buttons, selected card ringed); sidebar Symbol dropdowns removed from all five symbol-scoped modules. Screens built against the captures — Foreign Flow (Altair cumulative area + sign-colored daily bars, stats/KSEI panels, reversal callout, FGN/DOM split), Smart Heatmap (sector × stock tile grid + divergence panel), Accum Detect (combined price/accumulation canvas with stealth zone + VWAP reference), Sector Rotate (labeled quadrant map + sector cards), Risk Monitor (stat cards, positions table, crowding heat matrix, cap bars, scenario rows), login (hero + floating operator card). New `ui/charts.py` (Altair builders) + `ui/heatmap_view` grid/divergence rows. (c) **Streamlit 1.58 rail-click fix** (`7118dd8`): 1.58 sized the overlay button to its content (collapsing the full-card selector to a 16×2px sliver); every layer of the overlay chain pinned absolute/full-size + markdown margin zeroed — verified with a headless-Chrome click test (overlay covers the full 380×100 card, click switches symbol). **RULE B unchanged throughout**: measurements + state words only, no score/rank/verb; P&L and free-float denominators withheld rather than faked. 35 new tests (391 → **426 total**); headless AppTest sweep across all modules + login green. | The `design/screens/` captures are the declared pixel-fidelity target; these closed the remaining visible module-screen gaps within Streamlit's native reach (HTML/Altair view helpers, no React/JS layer — §10 stack intact). | v1.2 (no bump) |
| 2026-07-03 | **`design/` replaced with the operator's "VectorLab" handoff bundle** (restructure only — zero visual/behavioral change): the old monolithic `design/README.md` is verbatim `design/SCREENS_terminal.md`, `README-login-session-gate.md` is verbatim `design/SCREENS_login.md`, the prototype is renamed `IDX Flow Terminal.dc.html` (byte-identical), and a new `design/README.md` index (constraints, tokens, reading order) fronts the bundle. Screen specs, prototype, and LOCKED_SPEC diffed byte-identical to the previous handoff, so the shipped UI already implements it — no code change beyond repointing doc/docstring references. The bundle's `PLAN.md`/`DATA_SOURCES.md` copies were **stale snapshots** (pre-dating the 2026-07-03 live probes and slice-11 landing) and were NOT adopted; repo root stays authoritative. | Same design, new packaging — adopting the bundle keeps the handoff index's cross-references resolvable; adopting its stale doc copies would have regressed verified feed contracts. | v1.2 (no bump) |
| 2026-07-08 | **Portfolio auto paper-trader = the multi-name forward run that makes §6 bind** (`ff8ba35`). `validation.runner` drives ONE symbol against a *static* portfolio+circuit, so the §6 caps/breakers it passes to `execution.order` never clamp. `portfolio_runner.run_portfolio_forward` adds the portfolio layer: per day **exits-first → mark+circuit → ranked entries**, each sized against a **live** book so the 10%/name & 30%/sector caps and −3%/−10% breakers finally bind. **Selection = emergent + internal-SMS priority** (operator decision): not every ARMED name — a name still needs a valid Spring/LPS trigger (R:R≥2:1); no gross-exposure target, deployment bounded only by the locked caps; higher internal-SMS wins a contested slot (RULE B: ordering only, score never surfaced). `MAX_CONCURRENT_POSITIONS` optional count cap defaulted OFF (`None`) — off changes no locked behaviour. Sizing base stays a fixed equity notional (§6 `equity×1%`), matching `runner` so a one-name universe **reconciles** exactly (§13, shared fill engine). Regime does NOT scale allocation here — deferred to `signals.regime`. No LD/weight/gate/RULE change. | v1.2 (no bump) |
| 2026-07-08 | **Market regime is OBSERVATION_ONLY and scales no allocation** (`ff8ba35` + `725bec9`). Whether the market is risk-on/off is a natural "how much to deploy" input, but scaling allocation by regime is a **new locked sizing decision** — it must survive forward paper before it may drive a rupiah (the RULE B / LD-8 discipline). So `signals/regime.py` only observes: `classify_regime` reads a coarse regime from a look-ahead-safe proxy return series (trend deadband ±2%, MA confirmation) and `725bec9` adds the **breadth confirmation leg** — store-level `market_breadth` (advance-decline: fraction of the universe advancing per day, missing≠zero) + `classify_market_regime` requires the equal-weight trend proxy **and** breadth to agree, so weak breadth vetoes a RISK_ON read even on a rising proxy (A-D divergence → NEUTRAL). Convergent Zweig (Four Percent + A-D) / Murphy (primary trend + breadth). It exposes **no allocation multiplier** and is wired into nothing in `execution.order`/`portfolio_runner` (guard-tested). Promotion path = pin the factor set, version-bump `LOCKED_SPEC.md` with a new LD, gate the multiplier behind the `ValidationLedger` like SMS. | v1.2 (no bump) |
| 2026-07-06 | **Foreign-Flow free-float bar + Money-Replay transport, against the refreshed VectorLab bundle** (UI layer only, no locked-behavior change). (a) `design/` synced to the updated handoff: new `STYLE_GUIDE.md` (verbatim pixel-token reference) + split-out `Login Session Gate.dc.html`, refreshed terminal prototype, README/SCREENS_INDEX/SCREENS_terminal, and updated broker-flow + accum captures; the bundle's root spec dupes (CLAUDE/LOCKED_SPEC/PLAN/DATA_SOURCES) were again NOT adopted — repo root stays authoritative. (b) **Foreign Flow "FOREIGN OWN vs FREE-FLOAT"** (design 04): `free_float_pct` (SCR-0) threaded onto `ForeignFlowSnapshot` → `ksei_panel`, and the panel now renders foreign own% *of* free-float% with the bar filling to own's share of the float (35% of 38% → 92%). Gated on both KSEI ownership + SCR-0 free-float being present — the current store has neither (`ksei_ownership` empty, `free_float` all NULL), so the bar stays absent until that data lands (missing ≠ faked); verified end-to-end by injecting a slice into a store copy. (c) **Money Flow Replay transport** (design 06): the bare playhead slider replaced with the design transport — a circular accent play/pause button + scrubber + start/day/end date scale (auto-advance one frame per rerun, best-effort within Streamlit's model), the right column rebuilt as the themed AT PLAYHEAD kv panel + a semantic-colored WYCKOFF PHASE box (a RULE-A label, never a number), and a new themed `charts.replay_price_flow` (price / volume / foreign·smart-money flow lanes). 3 new tests (429 → **432 total**); Foreign Flow + Money Replay re-verified headless (AppTest) against a copy of the live store. | v1.2 (no bump) |
