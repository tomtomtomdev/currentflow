# PROGRESS.md — CurrentFlow durable log

Durable record of shipped slices and each module's **validation state** (RULE B). Update this
whenever a slice lands or a module changes validation state. Pairs with `PLAN.md` (the plan) and
`LOCKED_SPEC.md` (the spec).

`PAPER_VALIDATION_MONTHS = 3` (default). A module cannot display a number until `VALIDATED`.

---

## Shipped slices

| Slice | Status | Date | Notes |
|---|---|---|---|
| Spec & docs (LOCKED_SPEC v1.1, DATA_SOURCES, screeners) | ✅ | 2026-07-01 | Spec locked; data layer mapped to Stockbit `exodus`; server-side screeners defined. |
| Companion scaffolding (PLAN, CLAUDE, PROGRESS) | ✅ | 2026-07-01 | This file + PLAN.md + CLAUDE.md created per spec §14. |
| UI design handoff (hifi) | ✅ | 2026-07-01 | `design/` — README + `.dc.html` prototype; all 8 modules, RULE A/B enforced, seeded mock. Spec in bundle verified identical to root. Reference only, not shipped. |
| 1 · Data layer + integrity checks | ✅ | 2026-07-01 | `ExodusClient` (broker_summary + ohlcv_foreign; 401 fail-loud, refresh, exponential backoff); DuckDB store keyed `(symbol,date,as_of)`, ingest-once; integrity TRADED/NO_TRADES/NOT_PUBLISHED/GAP; look-ahead-safe reads. 23 tests pass. Pending live: `login/v6`+MFA transport & empirical broker publish-latency pinning (LD-5, conservative next-day fallback in force). |
| 2 · Universe gate + Broker Flow Analyzer | ✅ | 2026-07-02 | §3 hard floor + Track A/B + ARA/ARB derivation + rebalance down-weight (`universe/`); Broker Flow Analyzer observation module (`signals/broker_flow.py`) + Streamlit view (`ui/`); SCR-0 wired via screener POST, cached to `scr0_eligible` with `as_of`; DAL adds `symbol_info`/`corp_actions`/`special_board`. 60 new tests (83 total). Armed-list alerts stay manual pending 2–4 wks signal-quality validation. |
| 3 · Foreign Flow Dashboard + Money Flow Replay | ✅ | 2026-07-02 | Foreign Flow observation module (`signals/foreign_flow.py`): NBSA magnitude/persistence/reversal, vs-20d multiple + z-score (measurements), %-of-float, market/sector tide, KSEI overlay. Money Flow Replay (`signals/replay.py`): per-frame historical `decision_ts` re-reads (D+1 09:15 WIB), audit-tested against live signals. SCR-1A wired + cached with `as_of`; new DAL feed `ksei_ownership` (fetch-time `as_of`). Both wired into Streamlit. 27 new tests (110 total). |
| 4 · Phase classifier + SMS (internal) + veto | ✅ | 2026-07-02 | RULE A Wyckoff gate (`signals/phase.py`, detector-fed: climax→range→spring/SOS+LPS/UTAD; only C/D tradeable); internal SMS with locked §4 track weights (`signals/sms.py`, number GATED); full §5 veto taxonomy (`signals/veto.py`); engine gate→score→veto→ARMED (`signals/engine.py`); RULE B switch (`validation/state.py`) — SMS/Rank ships components only (`ui/sms_view.py`). Institutional Accumulation Detector + Smart Money Heatmap observation modules; replay phase lane lit. SCR-1B/1C/2 wired + cached. Backtest deferred to slice 7 (shared fill engine). 45 new tests (155 total). |
| 5 · Stage-2 distribution / trap layer | ✅ | 2026-07-02 | §8 signal-decay observation layer (`signals/distribution.py`): PHASE_ROLLOVER (UTAD/distribution), NO_DEMAND (VSA), BEARISH_DIVERGENCE (price up while flow falls — best exit), FOREIGN_OUTFLOW (NBSA sell streak) — categorical severities, no number (RULE B). `TrapMonitor` unifies §5 veto + §8 decay; `ui/trap_view.py` ribbon wired into every built view. SCR-EXIT screener wired + cached to `scr_exit_distribution` with `as_of`; `exit_flags_for` scopes to open+ARMED. 17 new tests (172 total). |
| 6 · Sector Rotation Map + Portfolio Risk Monitor | ✅ | 2026-07-02 | Sector Rotation Map derived view (`signals/sector_rotation.py`): net-foreign flow by sector on the RS-vs-flow quadrant (LEADERS/EARLY_RECOVERY/DISTRIBUTION_WARN/AVOID, §9 labels), RS vs the universe equal-weight proxy, tide; look-ahead-safe, missing≠zero. Portfolio Risk Monitor observation (`signals/risk_monitor.py`): §6 exposure caps (10%/30%), sector HHI, same-bandar crowding matrix (broker-overlap cosine) + correlated-pair check (ρ≥0.7), β vs injected benchmark, historical VaR (95%·1d), days-to-exit, scenario stress, §6 circuit breakers. Positions are an input (fill engine→slice 7); P&L withheld without an entry price. Views `ui/sector_view.py` + `ui/risk_view.py` wired into Streamlit. 20 new tests (192 total). |
| 7 · Execution (trigger/tilt/fill/risk) | ✅ | 2026-07-03 | Technical trigger (`execution/trigger.py`): Phase C spring / Phase D LPS → entry-limit + invalidation stop + first target (AR high / measured move), **R:R ≥ 2:1 or skip** (LD-3). Fundamental tilt (`fundamentals/tilt.py`): MF combined-rank tercile → COMPOUNDER/NEUTRAL/SPECULATIVE ×1.0/0.75/0.5, negative-EBIT override, FLOW_ONLY dual-track (ROE proxy, never COMPOUNDER hold) — never a gate (§7). Order gen (`execution/order.py`): LIMIT-only, 1%-risk × conviction sizing, §6 exposure caps + circuit breakers. **IDX paper fill engine** (`paper/fill.py`, §12): lot=100, tick bands, ARA/ARB adverse-side reject, next-open + tiered slippage, limit discipline, full fee stack (commission+levy+VAT+0.1% sell tax), T+2 — the ONE shared engine. Exit mgr (`execution/risk.py`, §8): stop→target→trailing→signal-decay (divergence best exit), capital-first. SCR-3/SCR-4 wired + cached. 54 new tests (246 total). Forward-paper *run* + 2-yr backtest reconciliation deferred to slice 8; live fundamentals feed not yet wired (tilt pure over injected/SCR-4 values). |
| 8 · Paper-trade validation wiring | ✅ | 2026-07-03 | Paper-trade runner (`validation/runner.py`): walks engine→trigger→tilt→order→**shared fill engine**→risk/exit into closed `PaperTrade`s; `run_backtest` (batch) + `run_forward` (day-by-day) reconcile over identical data (§13). `PaperTrade` atom (`trade.py`) — P&L net of full fee stack from the fills' cash flows. §8 metrics (`metrics.py`): net-of-fee return/Sharpe/maxDD/hit-rate/turnover + `walk_forward_sharpe`; **IHSG benchmark refused**. Promotion engine (`promotion.py`): `ValidationLedger` is the server-authoritative sole writer — promotes OBSERVATION_ONLY→VALIDATING→VALIDATED only on ≥`PAPER_VALIDATION_MONTHS` **and** positive walk-forward Sharpe. Observation↔claim switch generalized (`state.gated_display`) across all 3 gated modules (SMS + new `ui/ranking_view` + `ui/daily_top_view`), all wired into Streamlit reading the ledger. 21 new tests (267 total). A real multi-month forward run (needs live transport + accrued time) deferred; production modules stay OBSERVATION_ONLY until an actual run promotes them. |
| 10 · Live DAL transport | ✅ | 2026-07-03 | Closes the standing deferral (not a spec §11 slice; no locked-behavior change, no bump). Keychain Bearer store (`dal/token_store.py`, macOS `security` CLI, zero new dep) — missing→None never a blank header, strips `Bearer ` prefix, injectable runner. httpx transport (`dal/transport.py`) binds onto the client's injected `Transport`/`PostTransport`: returns raw `Response` (client maps status), raises `TransportError` on network failure + `AuthError` rather than send a blank auth, reads token **fresh per request**. Session factory (`dal/session.py`, `build_live_client`) — the one production construction site (token_provider + optional re-paste refresh + transport); `session_status` masked no-network health. Operator CLI (`dal/login.py`): paste/status/check/clear. 14 new tests (301 total). **Operator action (out of code):** capture the own-session Bearer + `login paste`; then a real multi-month forward run can accrue → promotes modules past OBSERVATION_ONLY (RULE B) → opens LD-8. |
| 11 · In-app credential + MFA login | ✅ | 2026-07-03 | Spec v1.2 §9.1 — credential login replaces Bearer-paste as the primary auth surface. `dal/auth.py` (`AuthClient`) implements the **verified** §4.1 flow: `login/v6/username` → `mfa/…/challenge/{start,otp/send,otp/verify}` (caller **loops** send→verify until `CHALLENGE_FINISH`) → `login/v6/new-device/verify` → `{access,refresh,user}`; bad-creds/failed-OTP→`AuthError`, network/5xx→`TransportError`, **never logs** password/OTP/recaptcha/tokens. `token_store` extended: access+refresh session as one JSON blob under a second Keychain account; `access_token()` prefers the login session, falls back to the pasted Bearer. `build_live_client` reads `access_token`; `build_session_refresh` seam + `AuthClient.refresh` **fail loud** until the refresh route is captured. CLI `login` subcommand drives the OTP loop; `paste` kept as fallback. `ui/login_view.py` pure state machine (CREDENTIALS→OTP→FINISH, credentials/OTP transient, never rendered/logged); `ui/app.py` auth-gates the modules + sign-out; `run.sh` drops the fail-loud precondition (login form served instead). 23 new tests (324 total). **Live-gated (operator, not code):** reCAPTCHA-v3 *enforcement* probe (token carried as a pass-through param) + refresh-route capture — §4.1 open items, not guessed. Engine/gate/weight/RULE untouched. |
| 12 · Login loop fix + DAL network-error logging | ✅ | 2026-07-03 | Two ops/observability fixes; no locked-behavior change, no bump. **(a) Login `Event loop is closed` fix** (`ui/app.py`): `_run` now drives coroutines on ONE persistent per-session event loop instead of `asyncio.run` (which opened+closed a fresh loop per submit, orphaning the `AuthClient` httpx pool cached in `session_state` → RuntimeError on the 2nd submit). **(b) Centralized network-error logging** (`dal/netlog.py`, `log_net_error`): every DAL transport seam (`HttpxTransport.get/post`, `AuthClient._post`, `ExodusClient._request`/`_maybe_backoff`) now emits one greppable `net-error` line — method + path + `status`\|`err=<ClassName>` + coarse outcome. WARNING for retryable/transient (network blip, 5xx, 429, in-flight retry), ERROR for fail-loud/exhausted (401-after-refresh, unexpected status, retries out). Redaction preserved-by-construction: the helper takes no body/token and logs the exception **class name only**, never its message (which can echo a URL). 14 new tests (337 total) incl. secrets-never-logged + no-log-on-success. |
| 12b · reCAPTCHA v3 token capture | ✅ | 2026-07-03 | Ops/observability follow-on to slice 11/12; no locked-behavior change, no bump. **Closes the §4.1 reCAPTCHA enforcement probe:** confirmed from a successful-vs-failed own-session capture that `login/v6/username` **rejects an empty token 400 "Permintaan tidak valid"** — v3 is enforced, a pure-Python login is impossible. Chosen fork = **operator-assisted token** (not a headless browser — heavy dep vs stdlib-core posture): new `dal/recaptcha.py` renders a DevTools-console snippet (`grecaptcha.execute(SITE_KEY,{action}).then(copy)`) surfaced at the paste point in CLI `login` and the Streamlit sign-in form; operator mints a fresh (single-use, ~2min) token and pastes it. Site key pinned in `config.AUTH_RECAPTCHA_SITE_KEY` (public, client-side by design) + action `"login"`. Login views **refuse an empty token before any request fires** (`LoginController.submit_credentials` + CLI). `DATA_SOURCES §4.1` + `auth.py` docstring updated: enforcement resolved; `player_id` required-vs-optional and the refresh route remain the open items. 2 new tests (empty-token guard asserts no request fires; snippet carries site key + action); full suite green (`343 passed`, incl. pre-existing uncommitted `netlog` test edits not in this commit). Engine/gate/weight/RULE untouched. |
| 12c · No-browser login (recaptcha presence-only + stable player_id) | ✅ | 2026-07-03 | Ops follow-on to 12b; no locked-behavior change, no bump. **Reverses 12b's "reCAPTCHA enforced / pure-Python impossible" conclusion via live probe against the own account:** `login/v6/username` validates only that `recaptcha_token` is **present & non-empty**, NOT its content/freshness — a reused stale HAR token AND an arbitrary junk string both log in 200; only empty/absent → `400 "Permintaan tidak valid"`. So **no browser/console/bookmarklet/headless is needed**: the client sends a fixed placeholder (`config.AUTH_RECAPTCHA_PLACEHOLDER`). **`player_id` resolved = the device-trust anchor:** required (empty → 400); a *previously-verified* UUID → trusted-device **direct session, no MFA** (new nested shape `data.login.token_data.{access,refresh}` — `_session_from_data` now parses both it and the step-5 flat shape); a *fresh* UUID → new-device MFA branch. Approach = generate a UUIDv4 **once**, persist in Keychain (`KEYCHAIN_PLAYER_ID_ACCOUNT`), reuse forever: `KeychainTokenStore.player_id()` mints-on-first-read, survives sign-out `clear()` (device stays trusted), `clear_player_id()` forces a fresh MFA. First login on a machine does the OTP loop once; every login after is direct. Removed `dal/recaptcha.py` + the console-snippet UI (CLI `login` + Streamlit form). **Verified end-to-end live**: shipped `AuthClient` (placeholder recaptcha + trusted player_id) → real access+refresh session, no browser/OTP. `DATA_SOURCES §4.1` + docstrings updated; net −1 recaptcha test, +2 (placeholder-body + player_id-lifecycle), full suite green (344). Engine/gate/weight/RULE untouched. |
| 13 · First-run auto-bootstrap ingest | ✅ | 2026-07-03 | Ops/plumbing; no locked-behavior change, no bump. First authed run with an **empty store** auto-fetches initial data: `ingest/bootstrap.py` (`bootstrap_ingest`, pure/async) resolves the universe via **SCR-0's first production wiring** (`run_scr0`, cached to `scr0_eligible` ingest-once) then ingests 90d (`DEFAULT_DAYS`, moved here; CLI re-imports) per survivor via `ingest_symbol`, emitting per-symbol progress. `ui/app.py:_maybe_bootstrap` — the UI's **first network client** (`build_live_client`, no refresher = 401 fail-loud; transport opened/closed on the slice-12 session loop via `_run`) — keys on store emptiness (session-restored launches bootstrap too), one-shot per session, `st.status` + progress bar. AuthError → Keychain cleared → login form (flag reset); other `ExodusError` → partial summary + retry button, kept symbols stay (ingest-once resume), terminal never bricked. **Live verification exposed + fixed two slice-1/2 DAL wire defects** (see decisions log): screener + OHLCV pagination enforced server-side (results were silently capped), and `marketdetectors` aggregates any multi-day range → `broker_summary` is now single-day and the pipeline pulls one broker call per missing day (bars written last as the ingest-once commit marker). Verified live end-to-end: SCR-0 → 101 eligible; per-day broker rows + full-range paginated bars land in the store; UI glue verified headless (AppTest) on both the fires-on-empty and skips-on-populated paths. `./run.sh ingest` reworded as backfill/manual fallback. 12 new tests (363 total). Engine/gate/weight/RULE untouched. |
| Portfolio auto paper-trader + market regime | ✅ | 2026-07-08 | Portfolio layer over the single-symbol runner (`validation/portfolio_runner.py`, `run_portfolio_forward`): walks the candidate universe day-by-day — **exits first** (frees name/sector room + cash), **mark + §6 circuit state** from running realised+unrealised P&L, then **entries** ranked by INTERNAL SMS (RULE B: ordering only) against a **live** `Portfolio`+`CircuitState` so the §6 10%/name & 30%/sector caps and the −3%/−10% breakers finally *bind* (the single-symbol `runner` fed them a static book, so they never clamped). Deployment is emergent (no gross-exposure target); `MAX_CONCURRENT_POSITIONS` optional count cap defaulted OFF. Reconciles with `runner.run_forward` on a one-name universe (shared fill engine, §13). Market **regime observation** (`signals/regime.py`, OBSERVATION_ONLY): RISK_ON/NEUTRAL/RISK_OFF/UNKNOWN from a look-ahead-safe proxy return series; `725bec9` extended it to **trend + breadth** — store-level `market_breadth` (advance-decline fraction, missing≠zero) + `classify_market_regime` confirms the equal-weight trend proxy with breadth (convergent Zweig Four-Percent+A-D / Murphy primary-trend+breadth); weak breadth vetoes a RISK_ON read even on a rising proxy (A-D divergence → NEUTRAL). Exposes **no allocation multiplier**, wired into nothing — scaling allocation by regime is a deferred, spec-bump-gated decision (RULE B). 13 new tests (443 → **456 total**). Engine/gate/weight/RULE untouched; no locked-behaviour change, no bump. |
| ARMED watchlist per-name Track A/B | ✅ | 2026-07-10 | Made the ARMED-watchlist rail **spec-faithful to §3 track assignment** — it was hardcoded to score *every* name as Track B (`app.py` `_all_results(store, "B", …)`), a prototype shortcut that mis-scored large-caps (broker-concentration weight 35 instead of 20, foreign-flow lead thrown away). Now each name is assigned its own §3 track (**A xor B, never blended, never A∪B**) and evaluated once under it. New shared rule `universe/track.py` (`assign_track`, `resolve_track`) is the single source of truth — `universe/gate.py` now imports it (Track enum moved there, re-exported) so gate and watchlist **cannot drift** (reconciliation-tested). Track A needs LQ45/IDX80 membership **and** ADV ≥ 25 bn; index membership — a live `emitten/{sym}/info` snapshot, previously unstored — is now persisted in a new `symbol_index` roster (`SymbolIndexRow`, keyed `(symbol, as_of)`, look-ahead-safe read-latest), populated by a `refresh_membership` pass in the bootstrap + ingest CLI (overlay, not a gate: fetch failure → Track B, 401 still fails loud). **Missing membership → Track B** (never invents A from absent data). Watchlist cache re-keyed on the roster version so a reconstitution re-tracks names. **No spec bump** (conforms to §3; the hardcoded B was the deviation). **No RULE B change** — track alters only internal SMS weighting; the rail still shows the state word + component spark-bars, never the number (A-tracked rows RULE-B-tested). No §4 weight/threshold edits. Accepted effect: correctly-A large-caps get stricter without foreign flow, so the lone ITMG B-arm drops from the rail — correct, and it doesn't touch the validation ledger. Verified end-to-end on the real store (AMMN/ADRO → A; AALI stays B on thin ADV). 15 new tests (**473 total**, full suite green). |
| 9 · Scale / ML (gated) | ✅ | 2026-07-03 | ML layer built as a **harness gated shut** (LD-8), mirroring slice 8. LD-8 admission gate (`ml/admission.py`) — the ML analogue of RULE B: reads the server-authoritative `ValidationLedger`, admits ML only when the rules system (`sms`) is VALIDATED; `require_admission` raises and is called by every ML entry point. Purged + embargoed walk-forward CV (`ml/cv.py`): anchored forward folds, label-overlap purge, boundary embargo, insufficient-samples raises. Engineered features only (`ml/features.py`) = the §4 SMS component sub-scores (no new signal). Walk-forward Sharpe weight **optimizer** (`ml/optimizer.py`) — sole writer: coordinate ascent maximizing train-fold Sharpe, worst OOS test-fold Sharpe as the accept gate, preserves the locked §4 simplex (sum=100, LD-1 Track-B `foreign_flow`=0), proposes only + never degrades. No-hand-edit weight store (`ml/weights_store.py`): no raw setter, `apply_proposal` re-checks admission + refuses non-improving; `compute_sms(weights=…)` seam scores candidates without mutating globals. Doubly-gated ML ranker (`ml/ranker.py`): LD-8 admission + RULE B display withhold. View `ui/ml_view.py` + Streamlit "⚙ ML Layer 🔒" pane surfaces the gate. 20 new tests (287 total). **Deferral (unchanged from slice 8):** the gate opens only on a real multi-month forward-paper run (live transport + accrued time); until then the layer stays LOCKED by design. |
| 14 · v2 UI restructure (Signal Pipeline home + evidence tabs) | ✅ | 2026-07-13 | Rebuilt the terminal to the **VectorLab v2** handoff (`design/HANDOFF_v2.md`); **no locked-behaviour change, no bump** (presentation reorganization; RULE A/B and §9 module behaviour untouched). **Nav rail removed** — `shell.shell_css` hides the sidebar; the main column is flush-left. New **Signal Pipeline** is the sole top-level view: `ui/pipeline_view.py` (pure view-model) builds Track A / Track B lanes of candidate rows over **real `engine.evaluate()`** output — each row shows all four locked stages (`gate → phase → sig → veto`) + verdict, a candidate stopping at the first failing stage (downstream `NOT EVALUATED`). RULE A visible (phase cell rejects non-C/D before the signal cell); **RULE B holds by construction** — the signal cell shows a categorical `pass`/`low` + component observations, never the composite SMS number (tested: no `internal_score` leak). Clicking a row (or an ARMED-rail card) opens that name's **evidence tabs** (Broker Flow / Foreign Flow / Accum. Detect / Money Replay — the four existing renderers reused with `show_header=False`) under a contextual "Why {TICKER} …" header + `‹ Pipeline` back button. shell.py gains `pipeline_*` builders + `.cf-pipe*`/`_STAGE_STYLE`/`_RESULT_STYLE`. The **seven pre-v2 modules** (heatmap/sector/risk/sms/ranking/daily-top/ml) are **kept in code + tests but unlinked** from nav (see decisions log). **Gate cell = §3 liquidity-floor + track leg only; EXITED verdict + full §3 gate deferred to PLAN.md Phase 2** (portfolio-paper-trader plumbing + `evaluate_gate` assembly — hooks left in place, seam marked in `app.py:_candidate`). 10 new tests (7 `test_pipeline_view` + 3 `test_app_pipeline` AppTest routing); **497 total, full suite green**. |
| 15 · Fast Mode auto paper-trader (LD-11) | ✅ | 2026-07-14 | **Spec bump v1.4 / LD-11** (relaxes LD-3 for Fast Mode only). Operator-armed, hands-off auto paper-trader over the ARMED watchlist — **paper only; RULE A (phase gate) and RULE B (presentation gate) unchanged.** Entry geometry reuses `TriggerSignal` so downstream is untouched: `execution/trigger.fast_detect` + `TriggerKind.FAST_ARMED` — entry = ARMED-day close × `(1 + FAST_MODE_LIMIT_PREMIUM)` (marketable limit), stop = `rng.support × (1 − STOP_BUFFER)` (invalidation), target = resistance (C) / measured move (D), `rr` **computed but not gated** (`valid = stop < entry` only — the LD-11 relaxation); no coherent range → skip (missing ≠ invented), non-C/D still refused by RULE A. Reuses the existing auto-trader: `RunConfig.fast_mode` / `PortfolioConfig.fast_mode` select the fast entry, `_attempt_exit` **unchanged** (same §8 ladder, shared fill engine — reconciliation-tested), and the day-loop body is extracted into `portfolio_runner.step_day` reused by the batch loop **and** the live daemon. **First trade/position persistence in the store:** `paper_position` (durable open book incl. entry cash-flow/fee so net P&L reconciles), `paper_trade` (closed, idempotent insert), `fast_mode_state` (`enabled`, `since_date`) — `scheduler_runs` pattern. Driver = `validation/fast_mode.run_fast_mode_step` (no-op if disarmed; loads book → ARMED specs → `step_day` → persists → feeds `ValidationLedger.record_forward_paper`), fired by the slice-12 daemon (`FEED_FAST_MODE`, `DailyAt(SCHEDULER_FAST_MODE_TIME, prior_trading_day=True)` after `eod_ingest`; the ARMED filter + fast entry resolve **inside** the step at the look-ahead-safe `decision_ts`, not at real-time `now`) — the first scheduler job that *scores + executes*; fail-loud-401 + durable state come free from the tick loop. **Dedicated `fast_mode` lane** added to `GATED_MODULES` — Fast-Mode trades never promote `sms`/`ai_ranking`/`daily_top` (RULE B honesty: a different entry policy earns its own validation). UI: `ui/fast_mode_view.py` (open book, per-trade net-of-fee realized P&L = **observation**, `n/3` accrual) with the **aggregate** expectancy/hit-rate **withheld (`•••`)** via `state.gated_display`; arm/disarm toggle + book panel in `ui/app.py`. Resolves PLAN.md Phase 2's deferred **`EXITED`** verdict — `pipeline_view._row` reads closed `paper_trade` → `REV` (`⤶`) cell + realized P&L, `shell._RESULT_STYLE["EXITED"]`. CLI `run.sh fast {enable\|disable\|status\|run}` → `python -m currentflow.fast`. 17 new tests (entry needs no trigger / ignores the R:R gate / skips incoherent geometry + non-tradeable phase; portfolio↔`run_forward` exit reconciliation; persistence across steps; disarmed no-op; sector cap still binds; scheduling + disarmed scheduler action; lane isolation; aggregate withheld; EXITED P&L with no score leak). |
| 16 · Haste Mode auto paper-trader (LD-12) | ✅ | 2026-08-09 | **Spec v1.5 / LD-12** (docs landed 2026-07-14; code this slice). Haste Mode is **Fast Mode with a wider cohort** — it drops the `ARMED@70` arming cut and auto-enters `WATCH ∪ ARMED` at *any* internal SMS. **Paper only; RULE A (phase gate), §5 veto and RULE B (presentation gate) unchanged.** **The firewall is structural, not a check:** the cohort is one shared predicate, `runner.in_entry_cohort(res, include_watch)` — `res.armed` by default, `res.state in {ARMED, WATCH}` under Haste — and `GATE_REJECTED`/`VETOED` are distinct engine states **upstream** of that split, so neither auto-trader nor the standard path can reach a gate-rejected or vetoed name (tested for all three cohorts on labelled fixtures). One definition feeds **both** entry sites (`runner._attempt_entry`, `portfolio_runner._rank_candidates`) so they cannot drift; `RunConfig.include_watch` / `PortfolioConfig.include_watch` are the only new knobs. Entry geometry, §6 sizing/caps/circuit-breakers and the §8 exit ladder are **byte-identical to Fast** (reuses `fast_detect`, `FAST_MODE_LIMIT_PREMIUM`, `STOP_BUFFER`, `TARGET_MEASURED_MOVE_MULT`); reconciliation with `runner.run_forward` re-verified on a one-name Haste run (shared fill engine, §13). **Persistence:** `paper_position`/`paper_trade` gain a leading `mode` discriminator (PKs now `(mode, symbol)` / `(mode, symbol, entry_date, exit_date)`) and `fast_mode_state` is re-keyed from the `"singleton"` row to one row per mode — so each mode has its own book, trade record, `since_date` RULE B clock and §6 circuit state, and a Haste position can never be exited or accrued as a Fast one. `CREATE TABLE IF NOT EXISTS` cannot add a PK column to an existing DB, so `Store._migrate_paper_mode` rebuilds the two tables in place on open, tagging pre-existing rows `FAST` (they *are* Fast trades) — idempotent, row-preserving, verified against a simulated slice-15 database. Driver/view/CLI are parametrized by mode rather than cloned: `run_fast_mode_step(…, mode=…)`, `accrue_mode`, `set_enabled(…, mode=…)`, `fast_mode_view.build_view(…, mode=…)`, and a shared `autotrader_cli` behind `python -m currentflow.fast` / `.haste` (`run.sh haste`). **Fast xor Haste:** they share one paper book and one §6 circuit budget, so `set_enabled` raises `ModeConflictError` when arming the second — fail loud in CLI (exit 1) and UI (toggle snaps back, warning shown), never silently resolved. `FEED_HASTE_MODE` fires at 09:12, a beat after Fast. **Dedicated `haste_mode` lane** in `GATED_MODULES` — Haste trades promote neither `fast_mode` nor the trigger-based modules, and the aggregate stays withheld (`•••`) until that lane validates; `app._fast_exits` unions both books so a Haste exit still reaches the pipeline `EXITED` verdict. 18 new tests (**623 total**, full suite green). |
| 17 · KSEI Institutional-Ownership Delta (LD-13a) | ✅ | 2026-08-09 | **Spec v1.6 / LD-13 (a) of three** — the bump was recorded 2026-07-15; this is its first vertical (data → signal → view → test). Wires the KSEI shareholder-composition feed (fetched + stored since slice 3, until now consumed **only** by the UI) into detection as **slow-money confirmation**. New `signals/ownership.py`: over the last `OWNERSHIP_WINDOW_SLICES` monthly slices → `ACCUMULATION_CONFIRMED` (share rising across the range), `DISTRIBUTION_TELL` (share falling **while price is marked flat/up**), `OWNERSHIP_EASING` (falling *with* the price — an exit, not dressing), `OWNERSHIP_FLAT`, `STALE_COMPOSITION`, `NO_COMPOSITION`. Categorical kind + INFO/WATCH/WARN severity (**salience**, not direction — the kind carries direction); no score, no verb. **The data cadence drives the design:** KSEI is monthly with an undisclosed lag (`as_of` = fetch time), so (i) a composition older than `OWNERSHIP_STALE_DAYS` **degrades to neutral** — a stale falling series can never flag distribution; (ii) `missing ≠ zero` — an unpublished `foreign_pct`, a lone slice, or an absent price window across the span is `available=False`, never a flat/0-strength reading; (iii) it corroborates, never decides. **§4 candidate at weight 0:** `ownership_delta` joins `COMPONENT_KEYS` and both tracks' `SMS_WEIGHTS` at **0** (simplex still sums to 100) — computed, observed, contributing exactly nothing; asserted running-score-unchanged both at `compute_sms` and through `engine.evaluate` before/after KSEI ingest. Deliberately **not** added to `ML_LOCKED_ZEROS`: LD-1's Track-B `foreign_flow` lock is *structural*, a candidate's 0 is merely *unearned*, so the walk-forward optimizer stays free to fund it (never a hand-edit, §4; RULE-B-gated). **§5 refinement:** `Veto.corroborators` + `veto._corroborate` attach the ownership note to a DISTRIBUTION_DRESSED veto that **already fired** on daily-cadence evidence — `None in → None out`, so the rejected set is byte-identical with and without the reading (tested on a clean Phase-C name carrying a textbook distribution composition: still no veto). `distribution.monitor` passes the same reading, so the trap ribbon and the pipeline never tell divergent stories about one veto. **View:** the KSEI overlay is promoted from a display sparkline to a categorical INSTITUTIONAL OWNERSHIP reading (`foreign_flow_view.ownership_panel`, severity-colored) in `app._render_foreign_flow` — i.e. the pipeline row's Foreign Flow evidence tab; the sparkline stays below it as the raw series. Panel copy is **digit-free by test** (a magnitude next to a live name is exactly what RULE B withholds); the pp measurement stays on the signal's `detail` for audit. `pipeline_view._COMP_LABEL` is deliberately untouched, so an unfunded candidate can never be named as a "lead component". 19 new tests (**642 total**, full suite green). RULE A, `ARMED@70`, active weights and RULE B all unchanged. |
| 18 · VPA bar-character (LD-13b) | ✅ | 2026-08-09 | **Spec v1.6 / LD-13 (b) of three** — no bump (v1.6 already carries it; §4.1/§9 pinned the design). Adds the one reading the framework turns on and the system did not have: **where the bar closed inside its own spread**. The §4 divergence spine reads effort (volume) against `|Δclose|` only, so a bar that opened at its high, sold off all session and closed on its low was indistinguishable from a quiet flat bar at the same close. New `signals/vpa.py` classifies each bar as `NO_DEMAND` (narrow up bar, low volume, after a rally) / `NO_SUPPLY` (narrow down bar, low volume, after a decline) / `ABSORPTION` (heavy volume, lower close, finishes near its high, inside the range) / `STOPPING_VOLUME` (the same on a **new low**) / `SUPPLY_PRESENT` (heavy volume up bar closing in its lower third — effort up, no result) / `CHURN` (heavy volume, narrow spread) / `DEMAND_CONFIRMED` (heavy, wide, closes on the high) / `NEUTRAL` / `UNREADABLE`, plus the two effort-vs-result flags (`effort_without_result`, `result_without_effort`). **Calibration is relative by construction** (Coulling): spread and volume are read against the recent `VPA_CONTEXT_BARS`, never an absolute lot or rupiah threshold — tested to yield identical characters at 1k and 50m lots, and to call the *same* absolute volume heavy or not depending on the context. **`missing ≠ zero`:** a locked ARA/ARB print has no range for the close to sit in → `UNREADABLE` (reading it as "closed mid-spread" would invent the very datum the module measures); too short a calibration base is likewise UNREADABLE, and gap/non-TRADED bars are dropped loudly, never read as a zero-volume zero-spread bar. **RULE A untouched, structurally:** `PhaseEvent.corroborators` + `phase._corroborate` hang the event bar's effort-vs-result note (`vpa.corroboration`) on SPRING / SOS / LPS / UTAD / SELLING_CLIMAX, and that runs **inside `verdict()`** — after the classifier has chosen the phase and with `tradeable` derived from the phase alone — so a corroborator can annotate a decision but can never make one (`None in → None out`). Asserted across **every labeled phase archetype**: identical phase, tradeability, reason, range and event identities with and without a reading; a Phase C spring-on-stopping-volume draws "VPA confirms", a Phase D SOS printing churn draws "VPA does not confirm" **and stays tradeable**. **§4 candidate at weight 0:** `bar_character` joins `COMPONENT_KEYS`, both tracks' `SMS_WEIGHTS` at **0** (simplex still sums to 100) and `SMS_CANDIDATE_COMPONENTS`; graded on the accumulation side only (absorption / stopping / no-supply prints), the weakness side is a warning and never negative strength. Running score asserted unchanged at `compute_sms` and through `engine.evaluate`. Deliberately **not** in `ML_LOCKED_ZEROS` (unearned ≠ structural) and `pipeline_view._COMP_LABEL` is untouched, so an unfunded candidate can never be named a "lead component". **View:** `ui/vpa_view.py` + `shell.vpa_ribbon_html` — a glyph-per-bar VPA BAR CHARACTER lane + the latest bar's reading on the **Accumulation Detector** and **Money Flow Replay** evidence tabs (`app._vpa_panel`); on Replay it is rebuilt at each frame's historical `decision_ts`, so the ribbon rewinds with the playhead. Copy is **digit-free by test** — the close-position / spread / volume ratios stay on the signal for audit and never reach the screen. 31 new tests (**673 total**, full suite green), including bar-by-bar look-ahead safety (truncating history never changes an earlier bar's character). Not yet exercised on stored IDX bars — this checkout's store is empty, so calibration evidence on real data is still owed. RULE A, `ARMED@70`, active weights and RULE B all unchanged. |
| 19 · Approximate volume profile (LD-13c) | ✅ | 2026-08-09 | **Spec v1.6 / LD-13 (c) of three** — no bump (v1.6 already carries it; §4.1/§9 pinned the design). Wyckoff 2.0's signature addition, absent until now: volume-at-**price** rather than volume-in-time. `signals/volume_profile.py` spreads each daily bar's volume uniformly across its own high–low range into `VP_BUCKETS` buckets of the window's range → **POC** (heaviest bucket, ties to the lower price), **value area** (grown outward from the POC, heavier neighbour first, ties upward, to `VP_VALUE_AREA_PCT` = 70%), **HVN/LVN** (relative to the window's mean live bucket, adjacent same-kind buckets merged into regions). Deterministic by construction — the same bars always reproduce the same levels, so a Replay frame reproduces the live read. **§4.1 fidelity honesty:** a true POC needs intraday depth this system does not ingest, so `ANNOTATION` travels *on the dataclass*, renders on every surface, and the chart draws `POC~`/`VAH~`/`VAL~`. `missing ≠ zero`: non-TRADED/incomplete bars dropped loudly; too few bars / no range / no volume → `available=False` with **no POC at all**, never a midpoint stand-in; a locked print lands whole in one bucket (exact, not estimated). **RULE A untouched:** Spring@VAL / UTAD@VAH / LPS@POC confluence is attached by `phase._corroborate` *inside* `verdict()` — after the phase is chosen and `tradeable` derived from it — asserted identical across every labeled archetype, and it stacks with slice 18's bar-character note without displacing it. **§4 candidate `vp_confluence`** (the phase-bonus refinement: not *whether* a spring printed but *where in the profile* it did) enters `COMPONENT_KEYS` + both tracks' `SMS_WEIGHTS` at **weight 0** (simplex still 100) + `SMS_CANDIDATE_COMPONENTS`; graded on the accumulation side only (UTAD is a warning, never negative strength). Not in `ML_LOCKED_ZEROS` — unearned, not structural, so the optimizer stays free to fund it under RULE B. View: `ui/volume_profile_view.py` + `shell.volume_profile_html` (horizontal VAP lane, value-area band, POC picked out) + `charts.volume_profile_layers` overlaying the levels on the Accumulation and Replay price lanes; on Replay the profile is rebuilt at *each frame's* historical `decision_ts`, so the structure rewinds with the playhead. 32 new tests (705 total, suite green). |
| 20 · Regime-scoped backfill + point-in-time universe | ✅ | 2026-07-15 | Infra / cache-only (slice-10/12 posture); **no spec bump** (writes store only, never scores, RULE A/B untouched). Companion `REGIME.md`. `config.regime_start(track)` + boundaries (A 2024-01-01 / B 2024-07-01 / holdout 2026-01-01). `index_roster_pit` table + `universe/roster.py` loader (operator CSVs under `data/rosters/`; rejects overlapping periods + missing-source, fail-loud, dir-wide). `universe/track.resolve_track_pit` + `universe/pit.pit_universe` — reconstructs the offline-checkable §3 gate legs, **names the sink-less legs** (`unchecked_legs` = corp-action / suspend, never faked), records stopped names in `known_missing`, roster-gap day → Track B + `roster_gap_days` (no silent caps). Read-boundary clamp: `Store.read_daily_bars(clamp_regime=track)` floors at the boundary; `run_backtest` / `run_portfolio_forward` **refuse** a pre-boundary start (fail loud, boundary named; portfolio clamps at Track B). Backfill CLI (`ingest/backfill.py` + `run.sh backfill`) — SCR-0 seed from `regime_start(track)`→today via `ingest_symbol`, resumable/ingest-once, budget printed. 18 new tests, full suite green (bar 4 pre-existing `test_app_pipeline` failures = live-app DuckDB lock, unrelated). Renumbered from the plan draft's "Slice 17" (17–19 reserved for LD-13). |
| 21 · Pattern catalog + base-rate estimation (LD-14) | ✅ | 2026-07-15 | **Spec bump v1.7 / LD-14** (renumbered from the draft's LD-13/v1.6 — already taken). Companion `PATTERN-CATALOG-SPEC.md`. Event-cadence base rates — the throughput complement to LD-11/12. `signals/pattern_features.py` (pure §4 features reusing broker_flow/foreign_flow/phase/sms/distribution — one math). `pattern_catalog`+`pattern_instance` tables (versioned append-only; **null-attached write guard** rejects a stored rate without its `rate_uncond`, §5.4). `patterns/scan.py` (walks `pit_universe`; **refuses a non-PIT source** §5.1; overlap-collapse; look-ahead-safe), `patterns/outcome.py` (forward-bar resolution + **terminal outcomes counted** §5.6), `patterns/estimate.py` (n / hit rate / **Wilson 90% CI** / **unconditional null** / confounds [liquidity tier + regime; sector deferred] / OOS split / decay; deterministic). Catalog view (`ui/catalog_view.py` + `app._render_catalog`) — **P1–P4**: dedicated full-width research surface (no ARMED rail beside it), both rates shown, small-n interval-only, "no instances" not 0%, stability label on every card, no verb/composite. `pattern_oos_accrual` scheduler feed (`MonthlyAt`, cache-only). Six seed patterns; `dominant-broker-flip` + `stealth-divergence` reach ESTIMATED end-to-end. 21 new tests, full suite green. **RULE B extended (4th presentation category), not relaxed; no SMS weight/threshold/veto/gate change.** |
| 22 · Survivorship: LISTED board roster + measured bias | ✅ | 2026-08-09 | **Infra / cache-only** (slice-10/12/20 posture); **no spec bump** — loads operator reference data and *measures* a data gap; never scores, gates, arms, or puts a number on a live name. Companion `BACKTEST_PHASE0.md` §3.1 (task **E1**, the first step of its §5 build order — deliberately before any further backfill, since fetching the wrong symbol set makes every later pull wasted work). **The problem:** every `exodus` endpoint is symbol-addressed, so you cannot ask Stockbit what was listed in 2024 — a store-derived universe holds exactly the names the vendor still serves and silently deletes every delisted/suspended/force-delisted name, which on IDX are precisely the names a value or high-float screen surfaces. **The fix comes from outside the vendor:** the operator transcribes IDX Statistics annual books to `data/listings/*.csv` (local-only — `data/` is gitignored, nothing republished); `universe/listing.py` diffs consecutive snapshots into a **`LISTED` pseudo-index** in `index_roster_pit`, and the raw books are kept in a new `listing_snapshot` table so the market-cap size proxy survives the derivation (`market_cap_idr` NULL = unknown, never 0). **What the annual cadence can and cannot say travels with the data:** a run reaching the newest book stays open, one that stops closes at its **last observation** with the delisting window (`ANNUAL_SNAPSHOT precision`, both bounding dates) written into the period's own `source` — an annual book cannot date a delisting to the day, so no reader can mistake the boundary for an announcement-dated one. The *start* is exact when the book's `listing_date` is transcribed (optional column) and otherwise "first observed", counted in the derive report rather than silently equated; a relisting yields two periods with the later start **clamped** past the earlier end, because the books reprint the *original* listing date and it would otherwise swallow the years the name was gone. **`LISTED` is a listing fact, never index membership:** `assign_track` reads only `config.TRACK_A_INDEXES`, and `Store.roster_covers` is now **scoped to those indexes** — the one real interaction bug, since an unscoped 'any index' check would have let a loaded book silently clear slice 20's `roster_gap_days` and the Time Machine's roster caveat. **The bias is now a number, not a warning** (`universe/survivorship.py`): per day or span, listed vs recoverable → unrecoverable names + count share + cap-weighted share, look-ahead-safe (recoverability judged at that day's `REPLAY_DECISION_TIME` frame, per-day as_of filter). **Missing ≠ zero throughout:** no roster → `UNMEASURED` with every share `None` (never 0%), no caps in the books → count share only with the limit named, a partially-capped book → share over the known subset plus a caveat naming how many are missing. Surfaced downstream: `PitUniverse.unrecoverable` (listed, never ingested) kept **distinct** from `known_missing` (had bars, then stopped) — different causes, counted separately — and `PremiseReport.survivorship`, which replaces the premise harness's generic survivorship caveat with the measurement when a roster exists. CLI `./run.sh listings` is offline (no session, no network). 23 new tests (**728 total**, full suite green). **Owed, named not faked:** `data/listings/` is empty until the IDX Statistics PDFs are transcribed, so every measurement today honestly reports UNMEASURED. |
| Time Machine (terminal-wide as-of rewind) | ✅ | 2026-08-08 | UI / read-path only; **no spec bump** (a `decision_ts` is a read parameter — no gate, score, weight, or presentation state changes; RULE A/B untouched). Closes the gap that the whole look-ahead firewall was already built for but the terminal couldn't use: `ui/app.py` hardcoded `datetime.now()` at **18 read sites**, so only `replay` and the CLI runners could look back. New `ui/timemachine.py` (pure, no Streamlit) owns the one definition — `resolve(day, now)` → `combine(day, REPLAY_DECISION_TIME)`, i.e. **D's pre-open**, the same "acting on D" convention as `validation.runner._decision_ts` and `universe/pit.py` (asserted equal in test, and deliberately *not* replay's D+1 framing). App-side, `_decision_ts()` replaces every one of those `now()` calls and threads `asof_day` explicitly into the cached/pure helpers (`_watchlist_data` is re-keyed on the moment, so live and rewound rails never mix). **Leaks closed beyond the store reads:** `_symbols` filters `as_of < decision_ts` when rewound (a name first ingested later must not be listed and then rendered empty — survivorship); `_as_of`/`_ihsg`/sector-rotation's window take `_max_visible_date` instead of raw `max(date)`; the replay scrub is bounded to `d < asof` (each frame still re-reads at its OWN historical `decision_ts` — the rewind bounds the range, never relaxes the per-frame firewall); Track A/B resolves via `resolve_track_pit` (`index_roster_pit`) when a roster period covers the day, since the live `symbol_index` snapshot is stamped at ingest and cannot rewind. **Writes never rewind** — bootstrap/ingest, the Fast-Mode paper book + accrual, the catalog seed, and the login session keep real wall-clock `now()`, and the Fast-Mode arm toggle is **disabled** while rewound (a rewound screen must not drive live state). Fail loud, no silent clamp (REGIME.md §1): a pre-regime or future day is refused with the reason on screen, never substituted by the boundary; picker `min_value` = `min(regime_start(A), regime_start(B))`. Non-rewindable surfaces are **named on screen** (`timemachine.caveats` → the banner: roster staleness / roster-gap → Track B, the sink-less §3 legs from `pit.UNCHECKED_GATE_LEGS`, live Fast-Mode book + RULE B state + catalog base rates, survivorship, weekend rewinds) — the `pit.unchecked_legs` posture. Chrome: `shell.timemachine_banner_html` (divergence purple, never amber — it must not read as a RULE-B pill) + a `REWOUND to …· not live` top-bar chip replacing the pulsing live dot, so no rewound screen can be mistaken for live. Empty panels while rewound say **"had been published before … — not a gap and not zero flow"** (missing ≠ zero). 17 new tests (`tests/test_timemachine.py`: 8 pure + 9 AppTest). |
| 12 · Automated per-feed ingestion scheduler | ✅ | 2026-07-11 | Infra, not a spec §11 slice (slice-10 posture); **no locked-behaviour change, no bump.** Replaces the manual `run.sh ingest` / empty-store bootstrap with a headless daemon (`currentflow/scheduler/`) that fires each feed on its own cadence during Mon–Fri 09:00–16:00 WIB and **writes cache only** — never scores, never touches RULE A/B, `as_of` stamping unchanged, ingest-once preserved (a restart / holiday / double-tick is a cheap no-op). Three modules: **`schedule.py`** the declarative `FEED_SCHEDULES` cadence surface (`FeedSchedule(feed, cadence, scope)`; `DailyAt`/`WeeklyAt`/`Interval`; scope `UNIVERSE`/`ARMED_WATCHLIST`/`NONE`); **`calendar.py`** pure `next_fire`/`is_due` due-math (injectable clock) + the `is_trading_time`/`applies_now` weekday-window gate + `previous_trading_day`; **`runner.py`** the tick loop that resolves scope and dispatches due feeds **sequentially** through the **existing** ingest surface — `eod_ingest`(broker+OHLCV)→`ingest_universe`, `universe_screener`→`run_scr0`, `index_membership`→`refresh_membership`, `ksei_ownership`→`write_ksei_ownership`. EOD feeds fire in-window and fetch the **prior completed trading day** (matches `BROKER_CONSERVATIVE_AVAILABLE_TIME` — the look-ahead stamp already in force). Durable `scheduler_runs(feed, last_fired_at, rows_written, outcome)` table drives due-ness (latest per feed) + is the audit trail → survives restart, no double-fire. **Fail loud on 401**: `AuthError` propagates, nothing recorded (feed stays due), daemon exits 1 (headless can't do the OTP re-login); non-auth feed errors are logged + recorded ERROR and advance the clock (retry next cadence, not hammered). Entry point `python -m currentflow.scheduler` (`--once`/`--db`) + `run.sh schedule` + `deploy/com.currentflow.scheduler.plist` LaunchAgent (`RunAtLoad`+`KeepAlive`). **Scope: 5 of the plan's 8 feeds wired; 3 deferred** — `corp_actions` (an injected RULE-A gate input, not cached), `special_board`, and `symbol_info`-status have **no store table / no cache consumer** today; wiring them would invent a table or change RULE-A input sourcing, outside the cache-only charter (named in `schedule.DEFERRED_FEEDS`, no silent caps; one-line to add once a sink lands). 19 new tests (**490 total**, full suite green). Engine/gate/weight/RULE untouched. |

---

| 23 · Framework Lenses: the five frameworks read apart + confluence | ✅ | 2026-08-17 | **Presentation-only** (slice-14 nav posture); **no spec bump** — a new observation surface over existing engine output; nothing decides, scores, gates, or arms. **The ask:** the five source frameworks (Wyckoff · Wyckoff 2.0 volume profile · VPA bar character · Bandarmology · Greenblatt Magic Formula) reach the operator only *fused* into the §2 AND-chain, where a candidate stops at the first stage it fails — so a name the phase gate rejects becomes invisible to every other framework that did see something. Slice 23 adds the complement: **each framework read on its own terms, one switchable section each**, plus a **confluence** section for the names more than one flags. `signals/frameworks.py` maps each framework to the signal it already owns (`phase.py` · `volume_profile.py` (+ its `confluences()`) · `vpa.py` · `broker_flow` component + `ownership` + the bandar-family §5 vetoes · `fundamentals/tilt.py` over SCR-4) and emits a `LensRead` — a **category and a sentence**, never a number. **RULE A holds structurally, not by test:** `read_symbol()` is a pure function of an already-computed `EngineResult` — a lens is downstream of a decision that has already been made, so it cannot arm a name, reopen the C/D gate, or reorder the pipeline; every row and every confluence row carries the engine's own verdict, and a name four lenses like that RULE A rejected renders as exactly that ("framework agreement never overrides that gate"). **RULE B holds by construction:** the only digits on the surface are **counts of symbols** — the confluence number is a **set size** (which lenses agree), the same species of fact as the pipeline's "3 armed · 2 watch"; it is never weighted, never ranked into quality, never multiplied into SMS (asserted: §4 weights byte-identical across a full lens+confluence pass). **Five states, not three** — `FLAGGED` / `CONTRARY` / `NEUTRAL` / `UNAVAILABLE` / `NOT_APPLICABLE`: "could not read this name" (no bars, no profile, no broker rows, no SCR-4 row) never renders like "read it and named nothing", and §7's deliberate abstention on FLOW_ONLY sectors (LD-7 skips Magic Formula on financials/utilities) is its own state rather than a silent neutral. Each section's census always names the unread count, including at zero. **Frameworks are allowed to disagree, on screen:** the ARMED archetype's flat high-volume cluster reads CHURN under VPA — the lens says so while the pipeline still arms the name, which is the point of the split. `ui/lens_view.py` (pure view-model) + `shell.lens_*_html` (a deliberately different palette from the pipeline's pass/fail marks — a lens read is not a stage verdict) + a full-width dedicated surface (`cf_view == "lenses"`, no ARMED rail beside it, same posture as the LD-14 catalog). Every row also names the *other* lenses that flagged the same name, so a section is never read in isolation. 34 new tests (**762 total**, full suite green), including the app-level open → switch all six sections → back routing. **Composition redesign shipped the same day** (`design/FRAMEWORK_LENSES_REDESIGN.md` + `design/framework-lenses-redesign.html`): a tab-card switcher carrying each lens's tally, rows grouped under **state bands** that print even at zero, a **fixed five-slot cross-lens strip** (`WYK · VP · VPA · BND · MF` — categorical, never a meter), dimmed-not-collapsed `N/A`/`UNREAD` rows, a persistent read-state key + column captions, and `RULE A · NOT TRADEABLE` as the loudest element of any confluence row the gate rejected. **Layout only** — tokens, states, copy and rules unchanged; 10 further tests (**773 total**, green). |
## Module validation state (RULE B)

State machine: `OBSERVATION_ONLY` → (accrues forward-paper) → `VALIDATING` → (≥ `PAPER_VALIDATION_MONTHS`
positive walk-forward) → `VALIDATED`. Only `VALIDATED` modules may show a number.

### Ships-now — observation modules (no gate; never show a predictive number)
| Module | State | Notes |
|---|---|---|
| Broker Flow Analyzer | ✅ built (2026-07-02) | Pure observation — net flow, DNA, top-N/HHI, persistence, syndicates, matrix. No score, no verb. |
| Foreign Flow Dashboard | ✅ built (2026-07-02) | Pure observation — NBSA series, persistence, reversal, splits, KSEI overlay. Multiples/z-scores are measurements, no score, no verb. |
| Institutional Accumulation Detector | ✅ built (2026-07-02) | Pure observation — stealth divergence, accumulator VWAP, volume dry-up + price tightness. Absorption needs L2 depth → `None` (graceful). No score. |
| Money Flow Replay | ✅ built (2026-07-02) | Audit tool — every frame re-read from the store at its historical `decision_ts`; reconciles with live signals (acceptance-tested). Wyckoff phase lane now lit by the slice-4 classifier (label only, `UNKNOWN` until enough history). |
| Smart Money Heatmap | ✅ built (2026-07-02) | Derived visualization — direction + intensity (flow-as-%-of-cap), sector drill-down, local-buy/foreign-sell divergence alerts. No score. |
| Distribution / Trap layer | ✅ built (2026-07-02) | Pure observation (slice 5) — §8 signal-decay flags: PHASE_ROLLOVER, NO_DEMAND, BEARISH_DIVERGENCE, FOREIGN_OUTFLOW. `TrapMonitor` unifies §5 veto traps + §8 decay; ribbon wired into every view. Categorical severities (INFO/WATCH/WARN), no number. |
| Sector Rotation Map | ✅ built (2026-07-02) | Derived visualization (slice 6) — net-foreign flow by sector on the RS-vs-flow quadrant (spec §9 labels), RS vs universe proxy, tide. RS/flow are measurements; the quadrant is a categorical observation. No score, no verb. |
| Portfolio Risk Monitor | ✅ built (2026-07-02) | Risk observation, not return prediction (slice 6) — §6 exposure caps, sector HHI, same-bandar crowding + correlated-pair check, β vs injected benchmark, historical VaR, days-to-exit, scenario stress (what-ifs), §6 circuit breakers. VaR/β/HHI are risk measurements; scenarios are hypothetical, never forecasts. No score, no verb. |
| Market Regime | ✅ built (2026-07-08) | Pure observation — RISK_ON/NEUTRAL/RISK_OFF/UNKNOWN from a look-ahead-safe equal-weight trend proxy **confirmed by advance-decline breadth** (Zweig/Murphy convergence); weak breadth downgrades a rising proxy to NEUTRAL (A-D divergence). Trend %/breadth are measurements; the regime is a categorical label. **No allocation multiplier — wired into nothing.** Promoting it to a gross-exposure input needs a spec LD bump + forward-paper validation (RULE B). |
| Institutional-Ownership Delta | ✅ built (2026-08-09) | Pure observation (slice 17, LD-13) — `signals/ownership.py`: KSEI shareholder-composition change across the accumulation window as slow-money confirmation (Bandarmology/Wyckoff CM). Categorical kinds (`ACCUMULATION_CONFIRMED` / `DISTRIBUTION_TELL` / `OWNERSHIP_EASING` / `OWNERSHIP_FLAT` / `STALE_COMPOSITION` / `NO_COMPOSITION`) + INFO/WATCH/WARN severity; the panel's copy is **digit-free**, no number, no verb. Monthly/lagged → slow confirmation, never a daily driver; stale (> `OWNERSHIP_STALE_DAYS`) degrades to neutral, `missing ≠ zero`. Also a §4 candidate SMS sub-signal **pinned at weight 0** (optimizer-only, RULE B) + a distribution-veto **corroborator that can never fire a veto alone**. |
| VPA Bar-Character | ✅ built (2026-08-09) | Pure observation (slice 18, LD-13) — `signals/vpa.py`: per-bar character from close-position-in-spread against relative spread + volume (Coulling): `NO_DEMAND` / `NO_SUPPLY` / `ABSORPTION` / `STOPPING_VOLUME` / `SUPPLY_PRESENT` / `CHURN` / `DEMAND_CONFIRMED` / `NEUTRAL` / `UNREADABLE`, plus the two effort-vs-result flags. Calibrated against the recent `VPA_CONTEXT_BARS`, never an absolute volume; a no-spread (locked) print or a too-short base is UNREADABLE, never "mid-range". Categorical + INFO/WATCH/WARN severity, no number; ribbon lane on the Accumulation + Replay evidence tabs (`ui/vpa_view.py`). Also the §4 `bar_character` candidate **pinned at weight 0** + phase-event corroboration (RULE A decision rule unchanged). |
| Volume Profile | ✅ built (2026-08-09) | Pure observation (slice 19, LD-13) — `signals/volume_profile.py`: **approximate** daily-bar volume-at-price → POC / VAH / VAL (70% value area) / HVN / LVN, each bar's volume spread uniformly across its own high–low range. Everything is expressed in buckets of the window's own range (scale-free); a locked print lands whole in one bucket; too few bars, no range, or no volume → **no profile at all**, never a fabricated midpoint (`missing ≠ zero`). **Fidelity honesty (§4.1):** `ANNOTATION` ("approximate — built from daily bars, not intraday depth") travels with the data and renders on every surface; chart levels are drawn `POC~`/`VAH~`/`VAL~`. Levels are prices, not claims — the copy is digit-free, no score, no verb. VP lane + chart overlay on the Accumulation + Replay evidence tabs (`ui/volume_profile_view.py`). Also the §4 `vp_confluence` candidate (the phase-bonus refinement: Spring@VAL / LPS@POC) **pinned at weight 0** + phase-event corroboration (RULE A decision rule unchanged). |
| Framework Lenses (5 frameworks + confluence) | ✅ built (2026-08-17) | Pure observation (slice 23) — `signals/frameworks.py` reads Wyckoff / Wyckoff 2.0 / VPA / Bandarmology / Magic Formula **apart** over an already-decided `EngineResult`, one section each + a confluence section. Category + sentence per lens, never a number; the confluence figure is a **set size** (how many frameworks agree), never a score and never fed into §4. `UNAVAILABLE` ≠ `NEUTRAL` ≠ `NOT_APPLICABLE` (missing ≠ zero). Decides nothing: every row carries the pipeline's own verdict, and agreement never overrides RULE A. |

### Gated modules — NO number until earned (RULE B)
| Module | Validation state | Paper-months accrued | Notes |
|---|---|---|---|
| Smart Money Score (SMS) | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 4): components shown as raw observation, internal 0–100 drives ARMED, **number withheld (`•••`)** by `validation.state` until VALIDATED. Slice 8: promotion now flows through the server-authoritative `ValidationLedger` (`validation.promotion`) — the sole writer; state stays OBSERVATION_ONLY until a real forward-paper run clears ≥3mo + positive walk-forward Sharpe. RULE-B-tested end-to-end. |
| AI Buy/Sell Ranking | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 8): `ui/ranking_view` — names ordered by internal flow strength ("flow-derived ranking, not a recommendation"); position + score **withheld (`•••`)** until VALIDATED via the ledger. Also gated by LD-8. |
| Daily Top Opportunities | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 8): `ui/daily_top_view` — "highest flow-signal names today," ARMED names surfaced with SMS *components* as observation; composite score **withheld** until VALIDATED. |
| Fast Mode Auto-Trader | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 15, LD-11): the operator-armed auto paper-trader over the ARMED watchlist (buys at once, no trigger — §6/LD-11). Open book + per-trade net-of-fee realized P&L show as **observation** (facts, entry price exists); the strategy's **aggregate** hit-rate/expectancy is **withheld (`•••`)** until the `fast_mode` lane clears ≥3mo forward paper + positive walk-forward Sharpe via the `ValidationLedger`. A **dedicated lane** — Fast-Mode trades never promote `sms`/`ai_ranking`/`daily_top`. |
| Haste Mode Auto-Trader | `OBSERVATION_ONLY` | 0 / 3 | Built (slice 16, LD-12): Fast Mode with a **wider cohort** — the operator-armed auto paper-trader over the `WATCH ∪ ARMED` watchlist, dropping the `SMS ≥ 70` arming cut (buys at once, no trigger — §6/LD-12). RULE A (phase gate C/D) + §5 veto still bind by construction, so no `GATE_REJECTED`/`VETOED` name is ever entered. Open book + per-trade net-of-fee realized P&L show as **observation** (facts, entry price exists); the **aggregate** hit-rate/expectancy is **withheld (`•••`)** until the `haste_mode` lane clears ≥3mo forward paper + positive walk-forward Sharpe via the `ValidationLedger`. A **dedicated lane** — Haste-Mode trades never promote `fast_mode`/`sms`/`ai_ranking`/`daily_top`. |

### Pattern catalog (LD-14, slice 21) — a distinct category, NOT a RULE-B-promotable module

The pattern catalog is **not** in the state machine above. Its numbers are **historical
frequency measurements about pattern classes** (like a z-score), confined to the dedicated
catalog view under **P1–P4** — they display legitimately *there* and only there, never on a
live candidate/pipeline/rail/evidence surface, never as a buy/sell verb or composite. Each
entry carries its own lifecycle status in `pattern_catalog.status`
(`FOLK→DEFINED→ESTIMATED→OOS_CHECKED`, or terminal `REJECTED`), independent of the RULE B
ledger. **Attaching a pattern's stats to a live *name* is a claim** and would go through the
standard RULE B path (a dedicated `ValidationLedger` lane) — the catalog itself promotes
nothing. Seeds at rest: four `DEFINED`, two (`dominant-broker-flip`, `stealth-divergence`)
reach `ESTIMATED` once run over backfilled data.

---

## Validation metrics log

Record forward-paper results here as they accrue (net of full fee stack, benchmarked to LQ45/sector).

| Date | Module | Ann. return (net) | Sharpe | Max DD | Hit rate | Turnover | vs benchmark |
|---|---|---|---|---|---|---|---|
| — | — | — | — | — | — | — | — |

---

## Decisions & deviations log

Record any spec deviation here with a reason and the spec version bump it triggered.

| Date | Change | Reason | Spec version |
|---|---|---|---|
| 2026-08-17 | **The terminal lands on Framework Lenses instead of the Signal Pipeline (chrome only; no spec bump), and a NULL index-membership row no longer crashes the track resolver.** (a) `app.DEFAULT_VIEW = "lenses"`, routed through a new **pure** `app._active_view(state)`: absent `cf_view` → the lens surface; a *present* `cf_view` (including the explicit `None` the `‹ Pipeline`/`‹ Catalog` back buttons write) wins, so leaving the landing view sticks for the session. The pipeline's `▸ Framework Lenses` button is kept. (b) `store.read_symbol_index_latest` reads a NULL/empty `indexes` as `()` rather than calling `.split` on it — the operator hit `AttributeError` at that line from a Streamlit run (`_render_lenses → _candidate → resolve_track`), and the crash was unreproducible against the current store (238 rows, 0 NULLs, column `NOT NULL`), so the read was hardened at the one value that can raise there. (c) The AppTest suites gained explicit `_pipeline_app()` navigation (`test_app_pipeline.py`, `test_timemachine.py`) instead of letting the moved landing view silently skip five pipeline-side assertions. | (a) The operator asked for it, and it is safe to grant precisely because a lens is a pure read over an already-computed `EngineResult`: which surface paints first cannot arm, un-reject, or re-gate a name, so RULE A stays structural and RULE B untouched (the lens surface still shows only categories, counts, and set sizes). The Signal Pipeline remains the **sole deciding** surface — the v2 handoff's "one place where decisions live" claim is about where verdicts are *formed*, not about which view opens first — and every lens row keeps carrying the engine's own verdict, so agreement can still never read as tradeability. Routing through a pure function keeps the default pinnable without a Streamlit session. (b) `()` is already the module's documented meaning for "no membership known", and `track.assign_track` reads it as Track B, so the degradation follows the existing missing-≠-zero convention: absent data can never be promoted into Track A. Fixing the read (rather than only the data) is what stops a torn or legacy roster row from taking the whole terminal down. (c) A moved landing view that turns pipeline tests into skips is exactly the silent-coverage-loss CLAUDE.md forbids; navigating explicitly keeps the pipeline, Fast-Mode and Time-Machine assertions live. **No locked behavior changes:** no decision (LD-1…LD-14), SMS weight, threshold, veto, phase gate (RULE A) or presentation gate (RULE B) is touched. | v1.7 (no bump) |
| 2026-08-17 | **Framework Lenses composition redesign — layout only, shipped the same day (`design/FRAMEWORK_LENSES_REDESIGN.md` + `design/framework-lenses-redesign.html`; no spec bump, no token/state/copy change).** (a) The six switcher buttons become **tab cards carrying each lens's tally** (`2 flagged · 0 unread`; confluence `2 symbols · widest 3`), clicked through an invisible overlay button rather than a native label. (b) Rows are grouped under **state bands** that render **at zero** (`no name in this state today — stated, not omitted`), and the per-row state *word* moves onto the band. (c) The variable cross-lens chips become a **fixed five-slot strip** (`WYK · VP · VPA · BND · MF`) at constant x-positions — filled = flagged, hollow = not, dashed = the section you are in. (d) `N/A`/`UNREAD` rows carry `opacity:0.72`; nothing collapses. (e) On a confluence row RULE A rejected, the **RULE A line becomes the loudest element** (own tinted cell, `#f85149`, 11px) and the agreement count is muted. (f) A persistent read-state key + column captions sit above every section. Nothing was removed — every datum on the old surface is still present. | (a) Section identity was weak and the day's census was only visible after switching; putting the tally on the card makes the switcher itself the census, and it stays RULE-B-clean because a tally is a count of symbols. (b) The bands are the **existing sort order made visible** — they assert nothing about which name is better — and printing an empty band is the same principle as the always-printed unread count: a state with no names today is a fact, not an omission. (c) Fixed slots make set membership column-scannable *without* becoming a magnitude: a name flagged by one lens and a name flagged by four produce identical geometry, only different slots, so no bar/ring/`3-of-5` dial can creep in (RULE B). Rows are still never sorted by slot count and slot width is never proportional. (d) De-emphasis without collapsing keeps five states five — dimming is the opposite of the missing-≠-zero failure, which is *merging* them. (e) The `PTRO` case: three frameworks liking a name the phase gate rejected must not read as encouragement. Agreement never overrides RULE A, so on screen it must never out-shout it either. (f) "Unread" must never be read as "found nothing", and the four columns were unlabelled. **No locked behavior changes:** the lens layer is still a pure read over an already-computed `EngineResult` — a lens switch moves one state variable and can never change a verdict. | v1.7 (no bump) |
| 2026-08-17 | **Slice 23 — the five frameworks get their own sections instead of only the fused gate chain (presentation-only; no spec bump).** (a) A new **dedicated full-width surface** (`cf_view == "lenses"`), not a sixth pipeline stage and not an evidence tab — reached by a button under the pipeline, alongside the Pattern Catalog. (b) Each lens is a **pure read over `EngineResult`**, never a re-computation from the store. (c) **Five lens states** (`FLAGGED`/`CONTRARY`/`NEUTRAL`/`UNAVAILABLE`/`NOT_APPLICABLE`) rather than flagged/not-flagged. (d) The **confluence count is set cardinality** and rows are ordered by it, with the engine verdict on every row. (e) The Bandarmology lens reads the **bandar-family §5 vetoes** (single-bandar monopoly, wash/churn, broker rotation, distribution-dressed) as its own CONTRARY; KSEI ownership colours the lens only when the broker leg named nothing. (f) A **different visual palette** from the pipeline's pass/fail stage marks. | (a) The v2 handoff makes Signal Pipeline the sole top-level view precisely so *decisions* live in one place; a per-framework observation is not a decision, so it takes the LD-14 catalog's posture — a dedicated surface with no ARMED rail beside it — rather than diluting the pipeline into a second grid the operator could mistake for a verdict. (b) Purity is what makes RULE A structural instead of tested: a lens is downstream of a decision already made, so it *cannot* arm, un-reject, or re-gate — no test could have given that guarantee as cheaply as the shape does. (c) Two states would have collapsed "this framework could not read the name" into "this framework saw nothing", which is exactly the missing-≠-zero failure CLAUDE.md forbids; and §7's LD-7 abstention on financials/utilities is a *deliberate* non-read, not an absence of data. (d) The operator asked for aggregation across frameworks, and a count of agreeing observations is the only form of it that stays RULE-B-clean — a weighted blend would be a second, unvalidated composite competing with SMS. Ordering by set size puts the widest agreement first without asserting it is the better trade, and carrying `GATE_REJECTED`/`VETOED` on the row keeps agreement from ever *looking* like tradeability. (e) Those vetoes are the Bandarmology framework's own disqualifiers — reading the footprint against itself — so hiding them under §5 alone would have made the lens flatter than the framework it represents; KSEI stays subordinate because its monthly cadence is far too coarse to call a name on its own (§4.1 (a)). (f) A lens read and a stage verdict must never look alike on screen — same colors would imply a lens cell passes or fails a candidate, which is the one thing this surface must not say. **No locked behavior changes:** no decision (LD-1…LD-14), SMS weight, `ARMED@70` threshold, veto, phase gate (RULE A), or presentation gate (RULE B) is touched — the surface only re-presents outputs the engine already produced. | v1.7 (no bump) |
| 2026-08-12 | **SCR-4's pinned payload corrected — a filter-less template returns no columns (`screeners.md` §1 + §SCR-4, `screeners/scr4.py`).** An audit of the operator's saved Stockbit screeners against `screeners.md` found two drifts, both fixed server-side, one of which was **also a latent defect in this repo**. (a) **SCR-1C** (saved copy only): the "effort present" leg `Volume > Volume MA 20 ×1.5` had degenerated to `Volume > 0`, a no-op on any traded name — the divergence proxy was matching flat-price names with no volume expansion at all (9 matches → **2** once restored: BAIK, JECX). `screeners/scr1c.py` **already carried the correct rule**, so the pipeline was never affected; only the UI copy was stale. (b) **SCR-4** (saved copy *and* `scr4.py`): the pinned payload `"filters":"[]"` is **unrunnable as specified** — exodus derives result columns from the **rules**, not `sequence`, and silently wipes `sequence` to `[""]` when `filters` is empty, returning 978 rows with an empty `results[]`. `run_scr4` would have set all six tilt inputs to `None`, leaving `fundamentals.tilt`'s COMPOUNDER/NEUTRAL/SPECULATIVE tercile nothing to rank. Each ranking column is now carried by a filter bounded at a **wide sentinel** (`NO_GATE_SENTINEL = "-99999999999999"`), measured live at **978 rows — identical to the unfiltered universe — with all six columns populated**. The saved copy had separately drifted to six `> 0` bounds (566 rows), which **did** gate: `EV/EBIT > 0` and `ROE > 0` exclude negative-EBIT names and loss-makers. **Never run against live data** (`scr4_fundamental_tilt` was empty, 0 rows), so no stored row or past signal is affected. Also pinned in `screeners.md` §1: the `sequence`⇄`filters` coupling, and the run-vs-save contract (`save:"1"` + `screenerid`; write keys are `item1name`/`item2name`, reads return `item1_name`/`item2_name`). Test `test_scr4_template_is_ranking_pull_no_filters` → `…_that_never_gates`, now asserting one filter per sequenced fitem, every bound at the sentinel, and no `> 0` bound may return. | The spec's SCR-4 payload could not do the job the spec assigns it: §7/LD-6 wants a **ranking pull that never gates**, but the literal `"filters":"[]"` yields no fundamentals to rank by, and the obvious repair (`> 0`) reintroduces gating — the drift found in the saved copy. Wide-bound filters are the only formulation the API supports that satisfies both halves, and it is verified by measurement (978 = the full unfiltered universe) rather than asserted. **No locked behavior changes:** §7's "fundamentals never block entry" is *restored*, not altered; no decision (LD-1…LD-14), SMS weight, `ARMED@70` threshold, veto, phase gate (RULE A), or presentation gate (RULE B) is touched — hence a companion-doc correction, not a `LOCKED_SPEC` bump. Recording the API constraint in `screeners.md` prevents the next filter-less attribute pull from repeating it silently (`missing ≠ zero`). | v1.7 (no bump) |
| 2026-08-09 | **Slice 22 — the point-in-time board comes from IDX's own annual books, not from the vendor (BACKTEST_PHASE0 §3.1 / E1; no spec bump).** (a) The `LISTED` universe is loaded from operator-transcribed IDX Statistics CSVs as a **pseudo-index inside `index_roster_pit`**, not a new table. (b) A delisting derived from annual snapshots closes at the **last observation**, with the uncertainty window carried in the period's `source` string rather than in a new schema column. (c) `Store.roster_covers` is **scoped to `config.TRACK_A_INDEXES`** instead of 'any index'. (d) A relisting's start is **clamped** past the prior period's end when the book reprints the original listing date. (e) Survivorship bias is reported as a **measurement or as UNMEASURED**, never as 0%. | (a) The table already has exactly the right shape (period + provenance + `as_of`), and reusing it means one read path, one overlap validator, and one Time Machine story; a parallel table would have duplicated all three. (b) The schema stays shared with the announcement-dated LQ45/IDX80 rows, and the weaker basis is impossible to read without also reading its precision — a `precision` column would be droppable, a source string is not. (c) `LISTED` says nothing about index membership; unscoped, one loaded book would have silently cleared slice 20's `roster_gap_days` and the Time Machine's roster caveat while Track A/B remained unreconstructable — a data load must not make a *different* gap disappear. (d) IDX books print the original listing date, so an unclamped second period would assert the name was listed throughout the years it was delisted — inventing the very history this slice exists to recover (and it would fail slice 20's overlap validator). (e) A bias share of 0% claims a complete universe; with no book loaded the truth is that the size of the gap is unknown, and CLAUDE.md's missing-≠-zero rule makes that distinction load-bearing rather than cosmetic. | (no bump) |
| 2026-08-09 | **Slice 18 implementation choices for the VPA bar character (all inside v1.6 / LD-13 (b) — no bump).** (a) **Character vocabulary extended past §4.1's three names.** §4.1 names "No-Demand / No-Supply / Absorption"; the module also emits `STOPPING_VOLUME`, `SUPPLY_PRESENT`, `CHURN`, `DEMAND_CONFIRMED`, `NEUTRAL`, `UNREADABLE`. (b) **Corroboration is attached inside `phase.verdict()`, not inside the detectors.** The plan asked for an effort-vs-result *input* to Spring/UTAD/SOS; it is wired as a note hung on the emitted `PhaseEvent`s after the phase is chosen. (c) **Zero-spread and short-base bars are `UNREADABLE`, not `NEUTRAL`.** (d) **Candidate graded on the accumulation side only** (absorption / stopping / no-supply prints ÷ `VPA_FULL_CREDIT_BARS`); the weakness side is surfaced as an observation and is never negative strength inside the simplex. | (a) The frameworks' effort-vs-result read is not exhausted by three labels, and a *named* character is what keeps the surface categorical — collapsing the rest into "Absorption" would have made one word mean four different bars. Each new name is still a word, never a number (RULE B). (b) Feeding a detector an input means the input could change the C/D verdict; attaching after the verdict makes RULE A neutrality **structural rather than tested** — `tradeable` is derived from the phase alone, so a corroborator provably cannot promote or demote a candidate. The acceptance test (every labeled archetype identical with the reading wired) then confirms what the structure already guarantees. (c) A locked ARA/ARB print has no range for the close to sit in; scoring it "mid-spread" would fabricate the exact datum the module exists to measure (`missing ≠ zero`). (d) Mirrors the slice-17 ownership candidate: the §4 simplex grades strength, and a warning is not negative strength — the weakness side belongs to the ribbon and (already) to the §5/§8 layers. | v1.6 (no bump) |
| 2026-07-15 | **LD-14 Pattern catalog (Slice 21) — event-cadence base rates, presentation-only.** Bandarmology/Wyckoff folklore → falsifiable, provenance-tagged pattern classes measured over a **point-in-time universe** (slice 20), each carrying `n`, hit rate, **Wilson 90% CI**, its **unconditional null**, confound strata, and an OOS split at the holdout seam. Confined to a **dedicated catalog view under P1–P4**: base rates never render on / next to / joined against a live candidate row, the pipeline, the ARMED rail, or an evidence tab (P1, grep-firewall-tested); no buy/sell verb, no cross-pattern composite, never multiplied into SMS (P2); **attaching a pattern's stats to a live name is a claim → the standard RULE B path** (a dedicated `ValidationLedger` lane), not the catalog (P3); small-n renders interval-only, an empty window renders "no instances" not 0% (P4). Store: `pattern_catalog`+`pattern_instance` (versioned append-only; the null-attached write guard rejects a stored rate lacking its `rate_uncond`, §5.4). Scanner is PIT-only by type (§5.1); terminal outcomes counted, never dropped (§5.6); estimator deterministic. Monthly cache-only `pattern_oos_accrual` scheduler feed. **RULE B is *extended* (a 4th presentation category), never relaxed; no SMS weight (optimizer stays sole writer), `ARMED@70` threshold, veto, phase-gate (RULE A), or existing presentation-gate behaviour changed.** | Trade-cadence validation is structurally starved (~1 armed trade/yr/32 names — the LD-11/12 deadlock); a pattern instance needs only a flag date + forward outcome, so event cadence yields hundreds of observations where the trade path yields single digits — giving §4/§5 an empirical footing **without touching weights or any gate**. A new displayed-number category warrants a documented bump. **Renumbered from the plan draft's "LD-13, v1.6": LD-13/v1.6 was already consumed by detection enrichment (row below), so the catalog is LD-14, v1.7.** | **v1.7 (bump)** |
| 2026-07-15 | **Regime-scoped backfill + point-in-time universe (Slice 20) — all historical computation clamped to `regime_start(track)` per new `REGIME.md`** (A 2024-01-01, B 2024-07-01 — operator decision 2026-07-15; two ⚠ VERIFY items on the exact IDX announcement dates remain operator actions). PIT universe (`universe/pit.py`) reconstructed from the store's own bars + the offline-checkable §3 gate legs; non-reconstructable legs (corp-action window, suspend/UMA — the slice-12 sink-less feeds) **named in `unchecked_legs`, never faked**; a name whose bars stop is recorded per day in `known_missing` (acknowledged delist bias, surfaced). PIT Track A/B from a reconstructed `index_roster_pit` (operator CSVs of IDX announcements; loader rejects overlaps + missing provenance); roster gaps → Track B + counted (`roster_gap_days`). Read-boundary clamp (`read_daily_bars(clamp_regime=…)`) + backtest-entry refusal of a pre-boundary start (fail loud, boundary named; portfolio clamps at Track B). Backfill one-shot CLI (`run.sh backfill`) over ingest-once (resumable by construction). **Cache-only infra, RULE A/B untouched.** Renumbered from the plan draft's "Slice 17" (17–19 reserved for LD-13). | Backtests/base rates over data generated under COVID/pre-FCA rules with current-regime constants are invalid; present-day SCR-0 universes are survivorship-biased. Current-regime scoping (vs era-versioned constants) accepted per REGIME.md §4. | (no bump) |
| 2026-07-15 | **LD-13 Detection enrichment (Slices 17–19) — three backtestable smart-money signals from data already held / OHLCV-derivable, added to §4 pinned at weight 0.** Grounded in the loaded Bandarmology / Wyckoff 2.0 / VPA frameworks and a code audit of the detection path, three signals the frameworks weight heavily are added: **(a) Institutional-Ownership Delta** — the KSEI shareholder-composition feed is **fetched + stored but consumed only by the UI** (verified: `foreign_flow.py:217` carries `ksei` into the snapshot; only `ui/foreign_flow_view`/`ui/app` read it — no SMS component, veto, or phase gate touches it), now wired as a slow-money accumulation confirmation + a distribution-veto corroborator (monthly/lagged data → slow confirmation, never a daily driver); **(b) VPA bar-character** (No-Demand/No-Supply/Absorption) — the close-position-within-the-spread is **absent** (verified: `_divergence` uses only volume vs `|Δclose|`; no `.high`/`.low`/spread/close-position anywhere in `sms.py`), added as phase-detector corroboration + a divergence refinement; **(c) approximate Volume Profile** (POC/VAH/VAL/HVN/LVN) — **does not exist** (verified: no `POC`/`value_area`/`volume_profile`/`vah`/`val` anywhere), added as a daily-bar VAP approximation feeding Spring@VAL / UTAD@VAH / LPS@POC phase context. **Nothing in the running score changes:** all three enter the SMS simplex **pinned at weight 0** (the LD-1 Track-B `foreign_flow=0` pattern); only the walk-forward optimizer (`ml/optimizer.py`) may raise a weight — never a hand-edit (§4) — and only after RULE B forward-paper earns it. RULE A's C/D decision rule is **unchanged** ((b)/(c) corroborate the detectors, never a new gate). Each ships **first as a RULE-B-clean observation module** (§9). Note also: broker **persistence** was audited and found to already exist (`broker_flow.persistence()` gates `_broker_concentration`) — the frameworks' "same broker over N days" footprint is already captured, so it is a *refinement*, not a gap, and is **not** part of this bump. **Live-only tape (L2 order-book absorption, running-trade "hajar kanan") stays fenced out of the validated score** — it cannot share the backtest fill engine (§13 parity, LD-5 posture) and remains observation-only. **No decision (LD-1…LD-12), SMS *active* weight, `ARMED@70` threshold, veto, phase-gate (RULE A), or presentation-gate (RULE B) behavior changed.** | The core detection (divergence spine + phase gate + broker persistence + NBSA foreign flow) is framework-faithful, but three signals the frameworks emphasize sit unused: KSEI ownership is paid-for-but-ignored by detection, VPA's close-in-spread is a real blind spot in the spine, and Wyckoff 2.0's Volume Profile is missing. Adding them **inert (weight 0)** enriches the §4 menu with **zero running-score change** until the optimizer + forward paper earn each one — the honest, no-hand-tuning path (RULE B / §4). Adding candidate components to the locked §4 menu warrants a documented bump per CLAUDE.md, even though every active weight/threshold/gate is untouched. | **v1.6 (bump)** |
| 2026-07-14 | **LD-12 Haste Mode (Slice 16) — operator-armed auto paper-trader over the WATCH+ARMED watchlist; drops the `ARMED@70` arming threshold for Haste Mode only.** Haste Mode is Fast Mode (LD-11) with a **wider candidate cohort**: it enters the `WATCH ∪ ARMED` set — every name that cleared the RULE A phase gate (C/D) **and** the §5 veto layer at *any* internal SMS — dropping only the `SMS ≥ 70` arming cut. Everything else is identical to Fast Mode: same triggerless marketable-LIMIT entry, same §6 sizing/caps/circuit-breakers, same §8 exit ladder, same scheduler-daemon driver + durable persisted book. **The safety boundary is provable in code:** `res.state` is `WATCH`/`ARMED` only *after* the phase gate + veto pass (`GATE_REJECTED`/`VETOED` are distinct upstream states), so neither auto-trader can ever enter a gate-rejected or vetoed name — **RULE A and every §5 veto hold by construction** (the cohort widens from `res.armed` to `res.state in {ARMED, WATCH}` at two filter sites; nothing upstream changes). Trades feed the server-authoritative `ValidationLedger` on a **dedicated `haste_mode` lane** — they do **NOT** promote `fast_mode` or the trigger-based modules (`sms`/`ai_ranking`/`daily_top`). One auto-trader (Fast **xor** Haste) armed at a time over the shared paper book. **Paper only — no live execution (§15). RULE A (phase gate), §5 veto, SMS weights, the `ARMED@70` threshold *definition*, and RULE B (presentation gate) all unchanged** — only the auto-trader's entry *cohort* is widened, Haste-only; the aggregate Haste number stays withheld until the `haste_mode` module validates. | Even Fast Mode's ARMED-only set arms too rarely (~1 trade/yr/32 names, 2026-07-08) for forward paper to accrue a statistically meaningful sample → RULE B can never promote → the LD-8 ML gate never opens (the standing deferral). The WATCH cohort is already phase-gated + veto-clean and far larger, so it supplies the validation throughput the whole RULE B / LD-8 path depends on, at zero live risk (paper). Overriding a locked entry cut (the `ARMED@70` gate) warrants a documented bump per CLAUDE.md; confining it to Haste Mode + a separate `haste_mode` lane keeps the Fast Mode / standard paths and every existing claim honest. | **v1.5 (bump)** |
| 2026-07-14 | **LD-11 Fast Mode (Slice 15) — operator-armed auto paper-trader over the ARMED watchlist; relaxes LD-3 for Fast Mode only.** When armed, Fast Mode buys **every ARMED name at once** on a marketable LIMIT **without** the Spring/LPS trigger and **without the R:R ≥ 2:1 gate** (R:R still computed, surfaced as observation); stop = Wyckoff range support (invalidation), target = range resistance / measured move (§6). §6 sizing/caps/circuit-breakers and the §8 exit ladder are **unchanged** ("exit strategy for this buy still applies the same"). Driver = a slice-12 **scheduler daemon** job (`fast_mode_autotrade`, `DailyAt` after EOD ingest) over a **durable persisted book** (new `paper_position`/`paper_trade`/`fast_mode_state` store tables — the first trade/position persistence in the store). Trades feed the server-authoritative `ValidationLedger` on a **dedicated `fast_mode` lane** — they do **NOT** promote the trigger-based modules (`sms`/`ai_ranking`/`daily_top`), which keep requiring trigger-based forward paper. Closed positions wire the deferred **`EXITED`** pipeline verdict + realized-P&L cell (resolves PLAN.md Phase 2). **Paper only — no live execution (§15). RULE A (phase gate) and RULE B (presentation gate) unchanged**; the aggregate Fast-Mode number stays withheld until the `fast_mode` module validates. | Reverses the standing deadlock: the trigger + R:R gate arms so rarely (~1 trade/yr/32 names, 2026-07-08) that forward paper can never accrue a statistically meaningful sample → RULE B can never promote any module → the LD-8 ML gate never opens. Fast Mode trades entry *precision* for the validation *throughput* the whole RULE B / LD-8 path depends on, at zero live risk (paper). Overriding a locked decision (LD-3) warrants a documented bump per CLAUDE.md; confining it to Fast Mode + a separate validation lane keeps the standard path and every existing claim honest. | **v1.4 (bump)** |
| 2026-07-13 | **v2 UI restructure (Slice 14): nav rail removed; Signal Pipeline is the sole top-level view; the four per-stock analyzers become evidence tabs; the other seven modules are unlinked from nav but kept in code + tests.** The design handoff was refreshed to VectorLab **v2** (`design/HANDOFF_v2.md` + updated `.dc.html` prototype). This reorganizes **navigation only** — RULE A still gates before scoring, RULE B still withholds every composite number/probability/verb (the new pipeline "signal" cell shows a categorical pass/low + component observations, tested for no `internal_score` leak), and the §9 modules' *behavior/validation state* is unchanged. **CLAUDE.md's "8 modules map 1:1 to §9" is superseded** by "Pipeline home + 4 evidence tabs; 7 modules retained but not top-level." The pipeline wires to real `engine.evaluate()` output (not mock). Two designed-for pieces are **explicitly deferred, not dropped** (PLAN.md "Phase 2 — pipeline plumbing to resolve"): the **EXITED** verdict (needs `validation/portfolio_runner` closed positions → `rev` cell + realized P&L; hooks already in the view-model/shell/`_count_str`, seam marked in `app.py:_candidate`) and the **full §3 Universe Gate** in the gate cell (today it derives the ADV-floor+track leg; history/gap/corp-action/ARA-ARB via `universe.gate.evaluate_gate` still to be assembled from the store). | UI/§9-nav change, not locked behaviour — RULE A/B (LD-2/LD-9) and every module's scoring are untouched, so it's a design-doc + PROGRESS decision, not a `LOCKED_SPEC` behaviour bump (per CLAUDE.md's "locked behavior requires a bump" — nav model isn't locked behaviour). Retaining the seven modules' code avoids discarding shipped, tested §9 work while matching the v2 nav. Recording the two deferrals here + in PLAN.md ensures the unconnected plumbing (EXITED / full gate) is resolved, not forgotten. | v1.3 (no bump) |
| 2026-07-11 | **Slice 12 scheduler wired 5 of the plan's 8 feeds; 3 deferred for lack of a cache sink.** `corp_actions`, `special_board`, and `symbol_info`-status have no store table and no cache consumer today: `corp_actions` is an **injected input to the RULE-A universe gate** (`universe/gate.py`), not persisted; `special_board` and the `symbol_info` status flags have a DAL method + parser but no consumer/table. Wiring them would either invent a store table or change how a RULE-A gate input is sourced (gate reads a cache instead of an injected value) — both outside a **cache-only** infra slice ("writes cache only; never touches RULE A/B"). They are named in `scheduler.schedule.DEFERRED_FEEDS` (no silent caps) and tested as unscheduled; adding one is a one-line `FEED_SCHEDULES` entry + a dispatch action once its sink lands. Also: `broker_summary`+`ohlcv_foreign` collapsed into one `eod_ingest` feed (they're fetched atomically by `ingest_symbol` — bars written last as the ingest-once commit marker — so scheduling them separately would double-drive `ingest_universe`). Non-auth feed errors advance the run clock (retry next cadence, not every tick) rather than hammer a failing/paywalled endpoint; the missed day is recoverable via the manual `./run.sh ingest` backfill. | The plan's 8-feed table was written before implementation; the honest boundary is "schedule what has a place to write." Deferring the 3 sink-less feeds keeps the slice cache-only and leaves RULE-A input sourcing untouched; documenting + testing them as deferred honors "no silent caps." | v1.3 (no bump) |
| 2026-07-08 | **§4 SMS component calibration fixed — the `ARMED@70` deadlock.** A full-year backtest over 32 liquid names (ingested 1yr history) surfaced that the internal SMS **never reached 70** (max 62.2 across 5,760 evals; p50 ≈ 35), so no module could ever accrue forward paper → RULE B could never promote → the whole validation path was blocked. Per-component funnel isolated the cause: **divergence** (weight 30, the spine) sat pinned near 0 (sub-score p90 0.11, max 0.41) and **block_trade** was pinned at 1.0 for everyone. Two scoring-shape defects fixed (`signals/sms.py` + `config.py`): **(a)** divergence was computed over the *whole* passed-in history → its flat-high-volume ratio diluted to ~0 for anything that trended; now over a **recent window** (`SMS_DIVERGENCE_WINDOW_DAYS=40`) with a **graduated** corr factor `_corr_factor` in [0.5,1.0] (reduces to the old binary rule at corr 0 and 0.3) replacing the ×0.5 cliff that fired on nearly every name (year-long vol/\|move\| corr ≈ 0.5). **(b)** block_trade's fixed IDR-1B floor OR'd with the %ADV test **saturated to 1.0** on any liquid name (a 1B buy is noise for a large-cap); now graded by max single-broker buy as a **fraction of ADV** (§4's "> 1% ADV"), IDR floor kept only as the ADV-unknown fallback. **RVOL investigated, left unchanged** — near-zero on quiet accumulation days is spec-faithful (it is the volume-*anomaly* component; low readings are correct). **§4 conditions (±0.5%, corr 0.3 reference), all six weights, and `ARMED@70` are untouched — no weight was hand-edited, no threshold moved.** Approach was **defect-fix only, explicitly NOT tuning to manufacture trades** (RULE B). Evidence post-fix (**in-sample backtest, does NOT promote**): max SMS 62→72; **1 name (ITMG) armed → valid trigger → round-tripped** 2026-06-10→11, net −286,910 IDR (−0.29%, SIGNAL_DECAY) — proving the full vertical (engine→trigger→order→shared fill→exit→net-of-fee `PaperTrade`) runs on real data. Arming stays **rare** (1 trade/yr/32 names — too sparse to forward-validate yet; walk-forward needs ≥6 trades); vetoes remain a tight secondary constraint (542/886 C/D vetoed). 3 new tests (456 → **459 total**). | The composite could not reach its own locked threshold on real data — a self-inflicted deadlock blocking every gated module. These are scoring-normalization corrections (how a component maps to [0,1]), the documented tunable surface, not weight/threshold edits; the fix was kept principled (recency, scale-relative, graduated) rather than fished to a trade count. Warrants a documented bump per CLAUDE.md since divergence's corr handling and block_trade's scoring shape are §4 behavior. | **v1.3 (bump)** |
| 2026-07-01 | Initial companion files created | Spec §14 requires PLAN/CLAUDE/PROGRESS | v1.1 (no bump) |
| 2026-07-01 | UI design handoff added under `design/` | Operator-provided hifi design target for §9 modules | v1.1 (no bump) |
| 2026-07-01 | Front-end stack = Streamlit (chose over React/hybrid) | Stay faithful to spec §10; single-user local tool; accept approximate fidelity | v1.1 (no bump) |
| 2026-07-01 | Store pinned = DuckDB (chose over SQLite) | Analytical (OLAP) workload — backtest, window-function aggregations (persistence/Herfindahl/NBSA), Replay range-scans over `(symbol, date, as_of)`; native Pandas/Polars/Arrow integration; nightly-batch single-writer ingest makes SQLite's OLTP/concurrency edge irrelevant. Within spec §10's SQLite/DuckDB allowance — pin, not a deviation. | v1.1 (no bump) |
| 2026-07-01 | Slice 1 impl choices: async DAL (injectable transport); timestamps WIB-local tz-naive; broker `as_of` = feed `data_last_updated` else conservative next-day 09:00 (LD-5) | Async matches DATA_SOURCES §6 surface + enables throttled concurrent paywalled pulls; single-exchange tz needs no zone mixing; conservative fallback keeps look-ahead honest until latency measured. Implementation detail within spec. | v1.1 (no bump) |
| 2026-07-02 | Dev-board ARA/ARB band (spec's "±10–25%" range) resolved by price tier: prev close ≥ 5000 → 10%, else 25%; ε = 0.5% absorbs tick rounding at the band; unknown board falls back to the tightest (main 7%) band | Spec §12 pins a range, not a rule; tiering mirrors IDX's tighter-band-for-higher-price convention; conservative fallback never hides a pinned close. Constants in `config.py`, tunable when measured against real ARA/ARB days. | v1.1 (no bump) |
| 2026-07-02 | Broker-DNA registry seeded from design handoff (KZ/AK/RX/ZP/YU foreign-inst, CC/NI/OD/DR local-inst, DX/AI/KI smart-money, BQ prop, YP/PD/CP/GR retail); fallback to feed's Asing/Lokal tag; unmapped local codes stay UNKNOWN | No served DNA feed; registry is operator knowledge, explicitly illustrative and overridable per call — never silently guessed. | v1.1 (no bump) |
| 2026-07-02 | Rebalance down-weight fires only when ALL of: β-residual ≤ 1%, tracker-broker flow share ≥ 50%, within ±7d of an MSCI review date (operator-maintained calendar in `universe/rebalance.py`) | §3 says down-weight pure-beta moves, not every move near a rebalance; conjunction keeps genuine alpha at full weight. Thresholds in `config.py`. | v1.1 (no bump) |
| 2026-07-02 | Replay frame for trading day D reconstructs `decision_ts` = D+1 09:15 WIB (`config.REPLAY_DECISION_TIME`, injectable) | First actionable pre-open moment at which both D's EOD bar (~16:15) and D's broker summary (LD-5 conservative D+1 09:00) are knowable; earlier moments would show broker lanes always empty. Revisit once publish latency measured. | v1.1 (no bump) |
| 2026-07-02 | KSEI ownership `as_of` = fetch time (not month-end) | KSEI publishes monthly with an undisclosed lag; stamping month-end would fabricate availability. Fetch time is the only honest availability claim — conservative by construction. | v1.1 (no bump) |
| 2026-07-02 | Foreign-vs-domestic "split" rendered as participation (turnover share) + signed nets; NBSA vs-20d multiple uses mean |net| (magnitude-robust) alongside a signed z-score | Per-stock net domestic ≡ −net foreign (two sides per trade), so a net-only split is a mirror; a signed 20d mean near zero makes the raw ratio explode. Both stats are measurements (RULE-B-safe), not scores. | v1.1 (no bump) |
| 2026-07-02 | Wyckoff classifier is **detector-fed & heuristic**: selling-climax/AR anchors the trading range (else a consolidation-base fallback); spring→C, SOS+LPS→D, UTAD/weak-up-bars→DISTRIBUTION, extension→E; thresholds in `config` (`PHASE_*`). SOS without a confirming LPS stays non-tradeable (B). | §2/LD-2 pins the *gate* (only C/D tradeable) but not an algorithm. Conservative-by-default (ambiguous → not tradeable) honors RULE A; thresholds are tunable against labeled real charts as the DNA registry is. | v1.1 (no bump) |
| 2026-07-02 | SMS components each normalize to a sub-score in [0,1]; SMS = Σ weightᵢ·subscoreᵢ × rebalance multiplier. Weights live in `config.SMS_WEIGHTS` (the only tunable surface). | §4 pins weights + the ARMED@70 threshold but not each detector's shape; normalization keeps the composite in 0–100 and each component independently observable (RULE B components). Weights isolated so only the walk-forward optimizer writes them. | v1.1 (no bump) |
| 2026-07-02 | Markup-on-thin-volume veto fires only on **up** spikes (pump), not the spring's downward shakeout | §5 defines it as "price spiking on low value traded (pump signature)" — markup is upward. Using `abs(Δprice)` mis-vetoed the Phase C spring. Fix keeps the C event intact. | v1.1 (no bump) |
| 2026-07-02 | RULE B switch: `validation.state` (per-module state, default OBSERVATION_ONLY) is the server-authoritative gate; `ui/sms_view.score_display` returns `•••` until VALIDATED. Slice 8 promotes via the paper-trade engine. | LD-9 requires the observation↔claim switch to be server-authoritative, never a client toggle. Shipping the switch now (defaulted closed) satisfies §13's RULE B acceptance test; slice 8 only flips it. | v1.1 (no bump) |
| 2026-07-02 | Replay Wyckoff phase lane now carries the classifier **label** (RULE A verdict, not a number), read over a longer look-ahead-safe base (`REPLAY_PHASE_LOOKBACK_DAYS`); `UNKNOWN` when history is short | The lane was a placeholder pending the slice-4 classifier, which now exists. A label is a gate verdict, not a score — RULE-B-safe; longer base is needed for a range to form and stays `as_of`-filtered. | v1.1 (no bump) |
| 2026-07-02 | Slice 5 distribution layer = the **exit/decay** complement to slice-4 vetoes, not a re-implementation. Veto (§5) rejects an *entry candidate*; a decay flag (§8) warns on an *open/ARMED* name. New detectors: PHASE_ROLLOVER, NO_DEMAND (VSA), BEARISH_DIVERGENCE, FOREIGN_OUTFLOW. UTAD/distribution & dominant-broker-flip stay in the phase classifier / veto and are *surfaced* (not duplicated) via `TrapMonitor`. Flags are categorical severities (INFO/WATCH/WARN), never numbers (RULE B). Thresholds in `config.DECAY_*`, tunable against labeled charts. | v1.1 (no bump) |
| 2026-07-02 | SCR-EXIT scans IHSG server-side (per screeners.md); `exit_flags_for` intersects survivors with the operator's open+ARMED watchlist so "runs continuously over open + ARMED names" (spec §11) holds without a bespoke per-name screener call — off-watch survivors are logged, never silently dropped. | v1.1 (no bump) |
| 2026-07-02 | Slice 6 **Sector Rotation Map**: quadrant = sign pair of (relative strength, net-foreign flow) → LEADERS / EARLY_RECOVERY / DISTRIBUTION_WARN / AVOID (spec §9's own labels; the ≥0 side wins ties, no dead zone). Relative strength = sector mean return − universe equal-weight mean return (a **market proxy**, since no IHSG/index feed is ingested and §8 forbids IHSG as a *return benchmark* — here it only frames the RS axis). RS/flow are measurements, the quadrant a categorical observation (RULE B). A sector missing either axis carries `quadrant=None` (missing ≠ zero). Window in `config.SECTOR_WINDOW_DAYS`. | v1.1 (no bump) |
| 2026-07-02 | Slice 6 **Portfolio Risk Monitor** is observation-only per §9 ("risk observations, not return predictions"): VaR/β/HHI/crowding-ρ/days-to-exit/what-if impacts are risk **measurements**, never a confidence, probability, SMS, or buy/sell verb (RULE B-safe by tier). Scenario stress rows are defined hypothetical shocks (config-pinned), never forecasts. | v1.1 (no bump) |
| 2026-07-02 | **β vs benchmark is injected, not IHSG-fed** — no index (IHSG) DAL feed exists, so `build_risk_report` takes `benchmark_returns`; β is `None` without it (missing ≠ zero). `market_proxy_returns` (universe equal-weight mean) is offered as an honest labeled proxy and is what the UI passes; wire a real IHSG series when a feed lands. Design's "β vs IHSG" is a systematic-risk measurement, distinct from the §8 rule that returns are benchmarked to LQ45/sector — not a conflict. | v1.1 (no bump) |
| 2026-07-02 | **"Same-bandar" crowding = cosine similarity of two names' net-by-broker vectors** (from the look-ahead-safe broker-flow snapshot); the correlated-pair check (§6) flags pairs with ρ ≥ `CROWDING_CORR_THRESHOLD` (0.70) and names the shared lead broker. A cosine captures "the same syndicate drives both on the same side" (design's DX-shared example) directly from stored flow; the §6 exposure caps (10% name / 30% sector) and §6 circuit breakers (−3% daily / −10% drawdown) are pure functions over supplied portfolio + injected P&L. Positions are an **input** — the IDX fill engine that produces them is slice 7; P&L stays `None` until an entry price exists, so no fabricated return is shown. | v1.1 (no bump) |
| 2026-07-02 | **2-yr backtest deferred from slice 4 to slice 7** | §11/§13 require backtest and forward-paper to *share one fill engine* and report net-of-full-fee-stack returns; the IDX fill engine lands in slice 7. Running a P&L backtest now (no fills/fees/exits) would violate that discipline and produce a number the spec forbids. Engine + phase are already look-ahead-safe and replay-auditable in the interim. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **fee stack** modelled as four explicit auditable components: broker commission (side-specific 0.15% buy / 0.25% sell, honouring §12's "~0.15–0.25%" range), IDX/KPEI/KSEI levy (~0.043%, both sides), PPN VAT 11% on the commission, and the 0.1% final sales tax on the sell notional only. | §12 lists the stack as "broker + levy + VAT + 0.1% sell tax"; decomposing each line makes the §13 hand-checked cases pin every component and keeps the "full fee stack" auditable. Rates in `config.FEE_*`, tunable to the operator's actual broker schedule. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **tick bands** = current IDX fraksi-harga regime (<200→1, 200–<500→2, 500–<2000→5, 2000–<5000→10, ≥5000→25); **slippage** tiered by ADV (≥100bn LARGE 0.1% / ≥25bn MID 0.35% / else SMALL 1.2%, midpoints of §12's LQ45/mid/small ranges), buys slip up & sells slip down, unknown ADV → SMALL (most conservative). | §12 pins lots/bands/slippage ranges but not exact ticks or the tier cutoffs; the fraksi table is the live IDX regime and midpoint slippage is the honest central estimate. Constants in `config.TICK_BANDS`/`SLIPPAGE_*`, tunable when measured against real fills. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **fill semantics**: next-open + adverse slippage, rounded to a valid tick, then **limit discipline** — a buy above its limit / a sell below it is an honest NO_FILL, and a fill slipped past the limit is capped at the limit (never exceeds it). ARA/ARB rejects only the **adverse** side (a buy into a locked limit-up = no offers; a sell into a locked limit-down = no bids); a buy into a limit-down is allowed. | LD-3: next-open + ARA/ARB make market fills fiction, so the engine only fills limits and never invents an execution. Modelling the adverse-side lock and the limit cap keeps paper fills fill-realistic (the §13 discipline the whole validation gate rests on). | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **trigger geometry**: Phase C spring → entry = spring-bar close, stop = spring low − `STOP_BUFFER`, target = range resistance (AR high); Phase D LPS → entry = LPS close, stop = LPS low − buffer, target = resistance + one range span (a Wyckoff measured move, since price already reached resistance in D). R:R computed on the actual entry limit; < 2:1 → SKIP. | §6 pins "close of spring-test / LPS pullback" as the trigger and "AR high / next HVN" as the first target with R:R ≥ 2:1, but not the exact levels; anchoring on the classifier's own range + event bars keeps the geometry structural and replay-auditable. Buffer/target-mult in `config`, tunable. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **exit priority** is capital-first: STOP → TARGET → TRAILING → SIGNAL_DECAY, evaluated on the latest bar (daily cadence re-evaluates each day); a bar spanning both stop and target assumes the worst (STOP). Any active §8 decay flag triggers a SIGNAL_DECAY exit (the decay detectors already encode §8's exit conditions), with the most-severe flag's detail surfaced. | §8 lists the exit triggers but not their precedence; protecting capital first is the conservative ordering, and reusing `signals.distribution` avoids re-implementing the §8 decay logic (divergence = the single best exit signal). | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **`PaperTrade` P&L is engine-derived, not recomputed** — net P&L = the two shared-engine fills' signed `cash_flow` sum (each already net of the §12 fee stack). No fee math is redone in `validation/`. | Makes "net of full fee stack" true by construction and guarantees backtest ⇄ forward reconcile (both build trades from the same fills); a second fee model would risk divergence the §13 reconciliation test exists to forbid. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **backtest ⇄ forward = two entry points, one engine**: `run_backtest` sweeps the window in a batch loop, `run_forward` steps day-by-day (flat→seek entry / holding→manage exit); both delegate to the same `_attempt_entry`/`_attempt_exit` (hence the same `paper.fill.fill_order` + `execution.risk`). Exit = market-style next-open sell modelled as a sell-limit at a sub-quote floor (always fills at next open net of adverse slippage; a genuinely locked ARB still REJECTS and carries a day). Per-day `decision_ts = combine(day, REPLAY_DECISION_TIME)` keeps every decision look-ahead-safe; the fill uses the day's known open. | §11/§13 pin "separate code paths sharing one fill engine; results reconcile" but not the loop shape; two real loops over shared helpers satisfy both the reconciliation invariant and the honest live-accrual vs batch distinction. Exit-as-limit-floor keeps the §12 next-open discipline without inventing a market fill (LD-3). | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **walk-forward Sharpe = worst of N sequential out-of-sample folds** (`folds=3`, min fold Sharpe; per-trade mean/σ, no annualisation — sign is what promotion reads); `None` when < 2·folds trades or any fold has zero dispersion. "Positive walk-forward" ⇒ every fold was risk-adjusted-positive, not just the aggregate. | §8 mandates walk-forward + out-of-sample and forbids a headline number until earned; requiring every fold positive is the conservative reading and resists a single lucky stretch carrying the record. Fold count tunable. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **promotion gate** (`validation.promotion.resolve_state`): OBSERVATION_ONLY when months=0; VALIDATED only when months ≥ `PAPER_VALIDATION_MONTHS` **and** walk-forward Sharpe > 0; otherwise VALIDATING (incl. enough months but non-positive/absent walk-forward). `ValidationLedger.record_forward_paper` is the ONLY writer — no public setter marks a module VALIDATED, so the observation↔claim switch stays server-authoritative (LD-9), never a client toggle. | §8's validation gate + LD-9's "paper-trade engine is the sole authority"; making time-accrued-but-unprofitable stay VALIDATING (never VALIDATED) prevents a module earning its number on tenure alone. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **IHSG benchmark refused at the metrics boundary**: `compute_metrics` raises on benchmark ∈ {IHSG, JCI, COMPOSITE, IDX COMPOSITE, ^JKSE}; Track A → LQ45, Track B → sector. | §8 "never IHSG as headline benchmark" — enforcing it where the number is produced makes an accidental composite benchmark impossible, not merely discouraged. | v1.1 (no bump) |
| 2026-07-03 | Slice 8 **`gated_display(module, value, registry, fmt)`** is the single observation↔claim primitive for every gated module (SMS/AI-ranking/Daily-Top); returns `•••` unless the module is VALIDATED (and always for a `None` value). `ui/sms_view` refactored onto it; ranking/daily-top view-models added; the Streamlit app reads `ValidationLedger.states()` (server-authoritative). | §13 requires the switch across *all* gated modules, not just SMS; one primitive keeps the withhold rule identical everywhere and removes the per-view WITHHELD duplication. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **ML layer built as a harness gated shut** (LD-8), mirroring slice 8's promotion harness. An admission gate (`ml/admission.py`) reads the server-authoritative `ValidationLedger` and admits ML only when the rules system (`sms`) is VALIDATED (≥`PAPER_VALIDATION_MONTHS` + positive walk-forward Sharpe). `require_admission` raises `MLNotAdmittedError` and is the first call in every ML entry point (optimizer, ranker, weight-apply). In production nothing has cleared forward paper, so the whole layer refuses to run — the honest state until a real forward run lands. | LD-8 defers ML until the rules demonstrably survive forward paper; making admission a server-authoritative read of the same ledger (never a client flag) is the ML analogue of RULE B, and shipping it closed-by-default satisfies "gated" without a live run. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **purged + embargoed walk-forward CV** (`ml/cv.py`): anchored forward folds (train strictly precedes test — the only honest OOS direction for a live-accruing strategy), **purge** of any train sample whose label span [t0,t1] overlaps the test window, **embargo** = `ceil(embargo_frac·n)` boundary samples dropped; unresolved labels (`t1=None`) treated as far-future ⇒ conservatively purged; too-few-samples raises `InsufficientSamplesError` (missing ≠ zero). | LD-8 mandates purged/embargoed CV + out-of-sample; anchored walk-forward + label-overlap purge + boundary embargo is the López de Prado discipline reduced to deterministic index math (no numpy/sklearn — the core stays stdlib per §10). | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **optimizer is the sole weight writer**: deterministic coordinate ascent on the integer weight simplex maximizing mean **train-fold** Sharpe; the **worst OOS test-fold** walk-forward Sharpe is the accept gate. Locked §4 structure preserved (weights sum to `ML_WEIGHT_SUM`=100, `ML_LOCKED_ZEROS` — Track-B `foreign_flow` — never funded). It **proposes only**; `improved` iff OOS>0 AND ≥ incumbent OOS AND changed. `compute_sms` gained a `weights=` override so candidates are scored without mutating `config.SMS_WEIGHTS`. | §4/CLAUDE.md: weights are the only tunable surface and only via walk-forward Sharpe — never hand-edited live. Search-fits-on-train / gate-on-OOS resists overfitting; proposing-not-mutating + the compute_sms seam make hand-editing structurally impossible. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **no-hand-edit weight store** (`ml/weights_store.py`): a provenance-tracked surface with **no raw setter** — the only mutation is `apply_proposal(proposal, ledger)`, which re-checks LD-8 admission and refuses any non-`improved` (degrading) proposal, recording provenance on every applied change. | "No live hand-editing of SMS weights" (§13 acceptance) is enforced by construction — there is no code path that accepts raw weights, and application is re-gated so a proposal existing never implies it may go live. | v1.1 (no bump) |
| 2026-07-03 | Slice 9 **ML ranker is doubly gated** (`ml/ranker.py`): a transparent linear ranker over engineered features, refusing to run without LD-8 admission AND withholding its score/position (`•••`) until the `ai_ranking` module is itself VALIDATED (RULE B). Ordering is observation ("flow-derived ML ranking"), the number is a claim. | §9/LD-8: ML also governs the AI ranker. Two independent server-authoritative gates (LD-8 admits the *layer* to run; RULE B gates whether *that module* may show a number) keep both invariants honest without conflating them. | v1.1 (no bump) |
| 2026-07-03 | Slice 10 **live DAL transport = paste-token + macOS Keychain** (not a spec §11 slice; closes the standing deferral). Bearer captured from the operator's own authenticated session and stored via the `security` CLI (subprocess, zero new dep — stdlib core per §10/slice-9 posture); httpx transport binds onto the client's already-injected `Transport`/`PostTransport` seam (returns raw `Response` so the client keeps mapping status codes; raises `TransportError`/`AuthError` per the contract; token read fresh per request so refresh needs no rebuild). `build_live_client` is the sole production construction site. | The client was transport-agnostic by design since slice 1; this only supplies the prod transport DATA_SOURCES §6 always assumed — no locked behavior changes. Keychain over a file honors CLAUDE.md local-first ("nothing leaves the machine, never republished") and avoids a plaintext on-disk secret; `security` over `keyring` avoids a new dependency. | v1.1 (no bump) |
| 2026-07-03 | Slice 7 **fundamental tilt injected, not DAL-fed** — no live-fundamentals feed is ingested yet (`fundamentals_live` is a future wire), so `classify_tilt` is pure over supplied MF rank / EV-EBIT / ROE (SCR-4 rows or injected values); missing MF rank → NEUTRAL (un-tilted default, missing ≠ zero), never bottom tercile. Fundamentals never block entry (LD-6) — the tilt only sizes conviction and hold horizon. | Mirrors the slice-6 injected-benchmark posture; keeps the tilt honest and testable now, and slice 8 / a fundamentals DAL feed can supply real point-in-time values (DATA_SOURCES §3.1) without changing the interface. | v1.1 (no bump) |
| 2026-07-03 | **Auth re-scoped: in-app username/password + MFA login flow** (planned slice 11) replaces the slice-10 out-of-band Bearer *paste* as the primary auth surface. Spec §9.1 rewritten; §10/§15 updated for credential handling (creds/OTP transient in-memory, never persisted/logged; only access+refresh tokens → Keychain). Wire contract **verified** from `login-stockbit.har` and pinned in `DATA_SOURCES §4.1`: `login/v6/username` → `mfa/verification/v1/challenge/{start,otp/send,otp/verify}` (verify loops until `CHALLENGE_FINISH`) → `login/v6/new-device/verify` → `{access ~24h, refresh ~7d}`. **Not yet built** — two open items gate code: (a) `recaptcha_token` is reCAPTCHA-**v3** (invisible — no challenge shown, only OTP; token silently browser-minted) — **probe server enforcement first** (login with token omitted/junk); pure-Python login works if unenforced, else headless-browser/assisted/paste fork; (b) refresh-endpoint route/shape not in the capture. Slice-10 paste remains the working fallback until settled. | Operator asked to sign in with credentials, not hand-capture a Bearer. Still own-session, local-only, nothing republished (LD-10 holds); no engine/gate/weight/RULE change — auth plumbing only. Reverses the explicit slice-10 "not a credential/MFA login" scope, so it warrants a documented bump per CLAUDE.md. | **v1.2 (bump)** |
| 2026-07-03 | Slice 12c **— live probe overturns the 12b reCAPTCHA conclusion; login is now no-browser.** Probing the own account showed `login/v6/username` validates `recaptcha_token` **presence only** (junk + reused-stale tokens log in 200; empty → 400), so the operator-assisted console-snippet fork (12b) was unnecessary — replaced by a fixed placeholder (`config.AUTH_RECAPTCHA_PLACEHOLDER`), `dal/recaptcha.py` + its UI removed. `player_id` resolved as the **device-trust anchor**: required, and a persisted UUID (minted once, `KEYCHAIN_PLAYER_ID_ACCOUNT`, survives sign-out) yields a trusted-device **direct session (no MFA)** on repeat logins; the new nested response shape `data.login.token_data.*` is now parsed. Verified end-to-end against the live endpoint. | Server-side reality beats the earlier inference: with no `siteverify` enforced, a pure-Python login is possible and the heavy/manual browser paths are pure cost. A stable local device id is the minimal way to keep the OTP a **one-time** event. Own-session, local-only, nothing republished (LD-10 holds); auth plumbing only — no engine/gate/weight/RULE change. | v1.2 (no bump) |
| 2026-07-03 | Slice 13 **first-run auto-bootstrap = SCR-0 universe + 90d ingest, fully automatic** (operator-chosen over a typed watchlist / confirm-first). `trading_day` for the `scr0_eligible` cache row = last-weekday proxy (the screener template carries no date — it runs "as of now" server-side, so the day is a storage key only). Error split: `AuthError` fails loud → Keychain session cleared → login form (the sign-out mechanism; persisted `player_id` keeps the device trusted, so re-login stays MFA-free); any other `ExodusError` (incl. paywall/rate-limit on what is the app's largest single pull, ~100–150 names × 2 requests) is **captured into a partial `BootstrapSummary`** — kept symbols stay stored, a retry button resumes via ingest-once, the modules always render. Sequential by design (pipeline pacing); `now` stamped once per run for consistent `as_of`. | v1.2 (no bump) |
| 2026-07-03 | Slice 13 **live probes corrected two pinned feed contracts** (DATA_SOURCES/screeners.md updated): (a) `screener/templates` REQUIRES an integer `page` (omitting → 400) with `limit` as page size — `run_screener` now sends `page`+`limit` (`SCREENER_PAGE_LIMIT` 900) and pages by `totalrows`, never a silent truncation; (b) `company-price-feed/historical/summary` caps un-paginated calls at ~12 rows regardless of range and rejects `limit`>50 — `ohlcv_foreign` pages (`OHLCV_PAGE_LIMIT` 50) until a short page; (c) `marketdetectors/{sym}` returns a **range AGGREGATE stamped `netbs_date=from` for any multi-day span** — per-day rows exist only day-by-day, so `broker_summary(sym, day)` is now single-day and `ingest_symbol` pulls one broker call per missing day, writing bars LAST as the ingest-once commit marker (deterministic `as_of` makes retry re-writes exact-key no-ops — no permanent broker holes). Cost model unchanged in kind (DATA_SOURCES §7 always budgeted per-day broker backfills); a 90d bootstrap ≈ 60 paywall-counted broker calls per name. Pre-fix multi-day broker ingests stored mis-dated aggregates — the only affected data (today's BBCA smoke rows) was purged with the db; re-ingest is clean. | v1.2 (no bump) |
| 2026-07-03 | Slice 11 **landed as a harness against the verified §4.1 contract** (injected-transport tests, no live network — the slice-8/9/10 posture). `AuthClient` posts unauthenticated JSON (no Bearer yet); the caller drives the send→verify OTP loop until `CHALLENGE_FINISH`. Two §4.1 open items are **carried, not guessed**: (a) `recaptcha_token` is a pass-through param — empty for the pure-Python attempt, an operator-minted token if the enforcement probe (still an operator action) shows it's required; (b) `config.AUTH_REFRESH_PATH=None` ⇒ `AuthClient.refresh` and the `build_session_refresh` seam **fail loud** (→ re-login), never a stale/empty fallback, until a real refresh exchange is captured. Session stored as access+refresh JSON under a second Keychain account; `access_token()` prefers it and falls back to the slice-10 pasted Bearer, so both auth paths coexist and the transport is unchanged. Login view-model is pure/tested; credentials/OTP/recaptcha never persisted, rendered back, or logged (asserted). | The verified contract is fully codeable now; the two unconfirmed items require live network + a real capture (operator, not CI), so they stay deferred exactly as slice-10's real forward run does. No LD/weight/gate/RULE change — auth plumbing only; gated modules stay server-authoritative via the ledger. | v1.2 (no further bump) |
| 2026-07-03 | **Design-fidelity pass against `design/` (audit-driven; UI layer only, no locked-behavior change):** (a) design tokens land as `.streamlit/config.toml` (bg `#070a10`/`#0d121b`, accent `#58c4dd`, sidebar `#0a0e14`, DNA/semantic chart palette, Geist/Geist Mono **when locally installed** — no runtime font fetch, local-first) and `run.sh` drops its `--theme.base` flag; (b) **ARMED watchlist rail** (`ui/watchlist_view.py`, sidebar-adapted): state WORD + the design's five spark strengths `DIV BRK FF RVOL BLK` — composite number/rank/verbs never emitted (RULE-B-tested), gate-rejected/vetoed names never listed, cap reported via `dropped` (no silent caps), one cached engine pass keyed on day+symbol-count; (c) **accumulation chart** (`accumulation_view.chart_rows`): price lane + accumulator-VWAP reference + cumulative smart-money lane, broker-less days render as a **gap, never zero**; raw-dict dump replaced with the design's "measured, not scored" stealth-metrics row; (d) **login State C + OTP footer**: in-app Bearer-paste card (`login_view.submit_bearer` + `dal/session.verify_bearer`) verifies with a live ping **before** storing — a rejected token is never written; OTP round gains channel select + resend (the idle `LoginController.send_otp`) with the server's `next_attempt_in` cooldown. Deliberately NOT chased (per the 2026-07-01 approximate-fidelity decision): custom top bar/nav rail/ticker chrome, replay autoplay, pixel-exact spark bars. 13 new tests (376 total); terminal + login states verified headless (AppTest) against a copy of the live store. | The design handoff is the pinned UI target; these were the audit's highest value-per-effort gaps *within* Streamlit's native reach — closing them without a React/JS layer keeps the §10 stack decision intact while restoring the design's tokens, rail, chart, and auth affordances. | v1.2 (no bump) |
| 2026-07-04 | **VectorLab bundle updated with pixel targets, and the shell chrome implemented against them.** The operator's refreshed `VectorLab.zip` adds `design/screens/` (ten 1280×800 captures — 2 login states + the 8 terminal modules) + `design/SCREENS_INDEX.md`, declared "the pixel-fidelity targets"; specs/prototype diffed byte-identical, and the zip's `PLAN.md`/`DATA_SOURCES.md` were again stale snapshots (pre-live-probe) and NOT adopted. The chrome the 2026-07-03 pass deliberately deferred is now shipped as **pure HTML/CSS view helpers** (`ui/shell.py` — tokens inlined, no Streamlit import, no React/JS layer, §10 intact): 52px top status bar (as-of stamp from the store's max ingested day, RULE-B pill, track chip, masked operator — no IHSG quote: the feed isn't ingested and a missing datum renders absent, never faked), module-header badge pills (green OBSERVATION / amber GATED / cyan DERIVED, ledger-driven on SMS), the **right-hand ARMED-watchlist rail** (moved out of the sidebar into a main/rail column split; five spark-bars DIV BRK FF RVOL BLK with exact values only in hover tooltips, missing component = empty slot "not available", cap note never silent), the amber **per-module validation-state bar** on SMS ("n.n / 3 months — number withheld" until the server-authoritative ledger says VALIDATED), and the 26px scrolling **§15 disclaimer ticker** (all five spec lines). RULE B by construction: no composite number, rank, probability, or buy/sell verb anywhere in shell output (tested). 15 new tests (391 total); all 11 modules + login re-verified headless (AppTest) against a copy of the live store. | The screens are the declared build target; the deferred chrome was the visible gap, and HTML string-builders keep it testable and inside Streamlit's model. | v1.2 (no bump) |
| 2026-07-04/05 | **Full module-screen fidelity pass against `design/screens/` (UI layer only, no locked-behavior change; 3 commits).** (a) **Chrome overrides + Broker Flow screen** (`fa11670`): hide Streamlit header/toolbar, restyle the sidebar radio into the design's nav rail; Broker Flow rendered with the design's stock header, HTML broker table (DNA chips, signed nets with under-bars, persistence dots), concentration panel, and the previously-missing §5 veto-checks panel; broker × stock matrix scoped to the (≤7) watchlist columns with green/red intensity cells — cap annotated, missing cells absent (never zero). (b) **Rail-card selection + styled module screens** (`7670784`): the ARMED-watchlist rail cards become the terminal's symbol selector (invisible full-card overlay buttons, selected card ringed); sidebar Symbol dropdowns removed from all five symbol-scoped modules. Screens built against the captures — Foreign Flow (Altair cumulative area + sign-colored daily bars, stats/KSEI panels, reversal callout, FGN/DOM split), Smart Heatmap (sector × stock tile grid + divergence panel), Accum Detect (combined price/accumulation canvas with stealth zone + VWAP reference), Sector Rotate (labeled quadrant map + sector cards), Risk Monitor (stat cards, positions table, crowding heat matrix, cap bars, scenario rows), login (hero + floating operator card). New `ui/charts.py` (Altair builders) + `ui/heatmap_view` grid/divergence rows. (c) **Streamlit 1.58 rail-click fix** (`7118dd8`): 1.58 sized the overlay button to its content (collapsing the full-card selector to a 16×2px sliver); every layer of the overlay chain pinned absolute/full-size + markdown margin zeroed — verified with a headless-Chrome click test (overlay covers the full 380×100 card, click switches symbol). **RULE B unchanged throughout**: measurements + state words only, no score/rank/verb; P&L and free-float denominators withheld rather than faked. 35 new tests (391 → **426 total**); headless AppTest sweep across all modules + login green. | The `design/screens/` captures are the declared pixel-fidelity target; these closed the remaining visible module-screen gaps within Streamlit's native reach (HTML/Altair view helpers, no React/JS layer — §10 stack intact). | v1.2 (no bump) |
| 2026-07-03 | **`design/` replaced with the operator's "VectorLab" handoff bundle** (restructure only — zero visual/behavioral change): the old monolithic `design/README.md` is verbatim `design/SCREENS_terminal.md`, `README-login-session-gate.md` is verbatim `design/SCREENS_login.md`, the prototype is renamed `IDX Flow Terminal.dc.html` (byte-identical), and a new `design/README.md` index (constraints, tokens, reading order) fronts the bundle. Screen specs, prototype, and LOCKED_SPEC diffed byte-identical to the previous handoff, so the shipped UI already implements it — no code change beyond repointing doc/docstring references. The bundle's `PLAN.md`/`DATA_SOURCES.md` copies were **stale snapshots** (pre-dating the 2026-07-03 live probes and slice-11 landing) and were NOT adopted; repo root stays authoritative. | Same design, new packaging — adopting the bundle keeps the handoff index's cross-references resolvable; adopting its stale doc copies would have regressed verified feed contracts. | v1.2 (no bump) |
| 2026-07-08 | **Portfolio auto paper-trader = the multi-name forward run that makes §6 bind** (`ff8ba35`). `validation.runner` drives ONE symbol against a *static* portfolio+circuit, so the §6 caps/breakers it passes to `execution.order` never clamp. `portfolio_runner.run_portfolio_forward` adds the portfolio layer: per day **exits-first → mark+circuit → ranked entries**, each sized against a **live** book so the 10%/name & 30%/sector caps and −3%/−10% breakers finally bind. **Selection = emergent + internal-SMS priority** (operator decision): not every ARMED name — a name still needs a valid Spring/LPS trigger (R:R≥2:1); no gross-exposure target, deployment bounded only by the locked caps; higher internal-SMS wins a contested slot (RULE B: ordering only, score never surfaced). `MAX_CONCURRENT_POSITIONS` optional count cap defaulted OFF (`None`) — off changes no locked behaviour. Sizing base stays a fixed equity notional (§6 `equity×1%`), matching `runner` so a one-name universe **reconciles** exactly (§13, shared fill engine). Regime does NOT scale allocation here — deferred to `signals.regime`. No LD/weight/gate/RULE change. | v1.2 (no bump) |
| 2026-07-08 | **Market regime is OBSERVATION_ONLY and scales no allocation** (`ff8ba35` + `725bec9`). Whether the market is risk-on/off is a natural "how much to deploy" input, but scaling allocation by regime is a **new locked sizing decision** — it must survive forward paper before it may drive a rupiah (the RULE B / LD-8 discipline). So `signals/regime.py` only observes: `classify_regime` reads a coarse regime from a look-ahead-safe proxy return series (trend deadband ±2%, MA confirmation) and `725bec9` adds the **breadth confirmation leg** — store-level `market_breadth` (advance-decline: fraction of the universe advancing per day, missing≠zero) + `classify_market_regime` requires the equal-weight trend proxy **and** breadth to agree, so weak breadth vetoes a RISK_ON read even on a rising proxy (A-D divergence → NEUTRAL). Convergent Zweig (Four Percent + A-D) / Murphy (primary trend + breadth). It exposes **no allocation multiplier** and is wired into nothing in `execution.order`/`portfolio_runner` (guard-tested). Promotion path = pin the factor set, version-bump `LOCKED_SPEC.md` with a new LD, gate the multiplier behind the `ValidationLedger` like SMS. | v1.2 (no bump) |
| 2026-07-06 | **Foreign-Flow free-float bar + Money-Replay transport, against the refreshed VectorLab bundle** (UI layer only, no locked-behavior change). (a) `design/` synced to the updated handoff: new `STYLE_GUIDE.md` (verbatim pixel-token reference) + split-out `Login Session Gate.dc.html`, refreshed terminal prototype, README/SCREENS_INDEX/SCREENS_terminal, and updated broker-flow + accum captures; the bundle's root spec dupes (CLAUDE/LOCKED_SPEC/PLAN/DATA_SOURCES) were again NOT adopted — repo root stays authoritative. (b) **Foreign Flow "FOREIGN OWN vs FREE-FLOAT"** (design 04): `free_float_pct` (SCR-0) threaded onto `ForeignFlowSnapshot` → `ksei_panel`, and the panel now renders foreign own% *of* free-float% with the bar filling to own's share of the float (35% of 38% → 92%). Gated on both KSEI ownership + SCR-0 free-float being present — the current store has neither (`ksei_ownership` empty, `free_float` all NULL), so the bar stays absent until that data lands (missing ≠ faked); verified end-to-end by injecting a slice into a store copy. (c) **Money Flow Replay transport** (design 06): the bare playhead slider replaced with the design transport — a circular accent play/pause button + scrubber + start/day/end date scale (auto-advance one frame per rerun, best-effort within Streamlit's model), the right column rebuilt as the themed AT PLAYHEAD kv panel + a semantic-colored WYCKOFF PHASE box (a RULE-A label, never a number), and a new themed `charts.replay_price_flow` (price / volume / foreign·smart-money flow lanes). 3 new tests (429 → **432 total**); Foreign Flow + Money Replay re-verified headless (AppTest) against a copy of the live store. | v1.2 (no bump) |
