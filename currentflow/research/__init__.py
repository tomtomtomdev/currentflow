"""Research layer — falsification tooling, never a live surface.

Nothing here may render on a candidate/pipeline/evidence view or feed SMS. Outputs are
*measurements* (like the pattern catalog's base rates, LD-14), not claims: no buy/sell
verb, no probability attached to a live name. The layer exists to kill components before
they are assembled, not to pick stocks.
"""
